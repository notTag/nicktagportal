---
phase: 999.8-unit-test-coverage
plan: 05
type: execute
wave: 3
depends_on: [999.8-02, 999.8-03]
files_modified:
  - apps/shell/src/views/__tests__/HomeView.test.ts
  - apps/shell/src/views/__tests__/SkillsView.test.ts
  - apps/shell/src/views/__tests__/CliView.test.ts
  - apps/shell/src/views/__tests__/PlaygroundView.test.ts
  - apps/shell/src/views/__tests__/NotFoundView.test.ts
  - apps/shell/src/layouts/__tests__/AppLayout.test.ts
  - apps/shell/src/router/__tests__/index.test.ts
  - apps/shell/src/__tests__/App.test.ts
autonomous: true
requirements:
  - COV-VIEWS
  - COV-ROUTER
  - COV-APP
must_haves:
  truths:
    - 'All 5 view components render without errors'
    - 'AppLayout renders TheHeader, RouterView, and conditionally TheFooter'
    - 'Router defines routes for /, /skills, /cli, /playground, and catch-all 404'
    - 'App.vue mounts AppLayout and initializes useTheme'
  artifacts:
    - path: 'apps/shell/src/views/__tests__/HomeView.test.ts'
      provides: 'HomeView component test'
      contains: 'HomeView'
    - path: 'apps/shell/src/views/__tests__/SkillsView.test.ts'
      provides: 'SkillsView component test'
      contains: 'SkillsView'
    - path: 'apps/shell/src/layouts/__tests__/AppLayout.test.ts'
      provides: 'AppLayout component test'
      contains: 'AppLayout'
    - path: 'apps/shell/src/router/__tests__/index.test.ts'
      provides: 'Router configuration test'
      contains: 'router'
    - path: 'apps/shell/src/__tests__/App.test.ts'
      provides: 'App.vue root component test'
      contains: 'App'
  key_links:
    - from: 'apps/shell/src/layouts/__tests__/AppLayout.test.ts'
      to: 'apps/shell/src/layouts/AppLayout.vue'
      via: 'mount with router and pinia stubs'
      pattern: 'mount.*AppLayout'
    - from: 'apps/shell/src/router/__tests__/index.test.ts'
      to: 'apps/shell/src/router/index.ts'
      via: 'import router'
      pattern: "router\\.getRoutes"
---

<objective>
Write unit tests for all 5 views, AppLayout, router configuration, and App.vue root component. Views are relatively simple (mostly composition of child components). Router tests verify route definitions.

Purpose: Cover the remaining shell app source files -- views, layout, router config, and root App component.
Output: 8 test files covering views, layout, router, and app root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
@.planning/phases/999.8-unit-test-coverage/999.8-01-SUMMARY.md

<interfaces>
From apps/shell/src/router/index.ts:
- Routes: / (home), /skills (lazy), /cli (lazy), /playground (lazy), /playground/:remote (lazy), /:pathMatch(.*)* (not-found, lazy)
- Uses createWebHistory('/') for history mode

From apps/shell/src/layouts/AppLayout.vue:

- Imports: TheHeader, TheFooter, SocialLinks, MobileMenu from @ui
- Imports: features from @/config/features
- State: isMobileMenuOpen ref, toggleMobileMenu/closeMobileMenu functions
- Template: TheHeader (with show-theme-picker, is-mobile-menu-open props), RouterView in main, TheFooter (v-if features.showFooter) with SocialLinks, MobileMenu outside app-content div
- Watch: sets inert/aria-hidden on #app-content when mobile menu opens

From apps/shell/src/App.vue:

- Imports AppLayout, useTheme
- Calls useTheme() in setup
- Template: <AppLayout />

From apps/shell/src/views/: (read each to determine exact content)

- HomeView.vue, SkillsView.vue, CliView.vue, PlaygroundView.vue, NotFoundView.vue
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test views and AppLayout</name>
  <files>
    apps/shell/src/views/__tests__/HomeView.test.ts,
    apps/shell/src/views/__tests__/SkillsView.test.ts,
    apps/shell/src/views/__tests__/CliView.test.ts,
    apps/shell/src/views/__tests__/PlaygroundView.test.ts,
    apps/shell/src/views/__tests__/NotFoundView.test.ts,
    apps/shell/src/layouts/__tests__/AppLayout.test.ts
  </files>
  <read_first>
    apps/shell/src/views/HomeView.vue,
    apps/shell/src/views/SkillsView.vue,
    apps/shell/src/views/CliView.vue,
    apps/shell/src/views/PlaygroundView.vue,
    apps/shell/src/views/NotFoundView.vue,
    apps/shell/src/layouts/AppLayout.vue,
    apps/shell/src/config/features.ts
  </read_first>
  <behavior>
    HomeView.test.ts:
    - Renders without errors (mount succeeds)
    - Contains expected content (read the source to determine what text/elements to check)

    SkillsView.test.ts:
    - Renders without errors
    - Contains SkillsToolbar and DiamondWall child components (use shallowMount)

    CliView.test.ts:
    - Renders without errors
    - Contains expected placeholder or terminal-related content

    PlaygroundView.test.ts:
    - Renders without errors

    NotFoundView.test.ts:
    - Renders without errors
    - Contains "404" or "not found" text

    AppLayout.test.ts:
    - Renders TheHeader stub (use shallowMount)
    - Renders TheFooter stub when features.showFooter is true
    - Renders MobileMenu stub
    - Has an element with id "app-content"
    - Renders a main element containing RouterView stub

  </behavior>
  <action>
Create `__tests__/` directories under views and layouts.

**For each view:** Read the source .vue file FIRST. Most views are simple templates that compose child components. Use `shallowMount` with `createTestingPinia()` and router stubs:

```typescript
import { describe, it, expect } from 'vitest'
import { shallowMount } from '@vue/test-utils'
import { createTestingPinia } from '@pinia/testing'
import { createRouter, createMemoryHistory } from 'vue-router'
```

For views that use the router (have RouterView or router-link), provide a minimal router:

```typescript
const router = createRouter({
  history: createMemoryHistory(),
  routes: [{ path: '/', component: { template: '<div />' } }],
})
```

Mount with `global: { plugins: [createTestingPinia(), router] }`.

**AppLayout.test.ts:** This is the most complex. It uses TheHeader, TheFooter, SocialLinks, MobileMenu from @ui, and features config. Use `shallowMount` so child components become stubs. Provide createTestingPinia and a router. Verify:

1. `wrapper.find('#app-content')` exists
2. `wrapper.findComponent({ name: 'TheHeader' })` exists (or find the stub)
3. A `<main>` element exists
4. MobileMenu stub exists in the output

For the mobile menu toggle: read the source to understand how the watch works, but do NOT test DOM manipulation on `document.getElementById('app-content')` -- that is an integration concern. Test that `isMobileMenuOpen` state changes are reflected in props passed to child components.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/views/**tests**/ apps/shell/src/layouts/**tests**/ --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `grep -q "HomeView" apps/shell/src/views/__tests__/HomeView.test.ts` returns 0 - `grep -q "SkillsView" apps/shell/src/views/__tests__/SkillsView.test.ts` returns 0 - `grep -q "CliView" apps/shell/src/views/__tests__/CliView.test.ts` returns 0 - `grep -q "NotFoundView" apps/shell/src/views/__tests__/NotFoundView.test.ts` returns 0 - `grep -q "AppLayout" apps/shell/src/layouts/__tests__/AppLayout.test.ts` returns 0 - `grep -q "app-content" apps/shell/src/layouts/__tests__/AppLayout.test.ts` returns 0 - All 6 test files pass: exit code 0
</acceptance_criteria>
<done>All 5 views tested for mount success and content verification. AppLayout tested for child component composition, app-content container, and footer conditional rendering. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test router configuration and App.vue</name>
  <files>
    apps/shell/src/router/__tests__/index.test.ts,
    apps/shell/src/__tests__/App.test.ts
  </files>
  <read_first>
    apps/shell/src/router/index.ts,
    apps/shell/src/App.vue,
    apps/shell/src/composables/useTheme.ts
  </read_first>
  <behavior>
    router/index.test.ts:
    - Router has a route for path '/' with name 'home'
    - Router has a route for path '/skills' with name 'skills'
    - Router has a route for path '/cli' with name 'cli'
    - Router has a route for path '/playground' with name 'playground'
    - Router has a route for path '/playground/:remote' with name 'playground-remote'
    - Router has a catch-all route for unmatched paths with name 'not-found'
    - Router uses createWebHistory with base '/'
    - Total route count is 6

    App.test.ts:
    - Mounts without errors
    - Contains AppLayout child component (use shallowMount)
    - Calls useTheme on setup (verify CSS variables are applied to document)

  </behavior>
  <action>
**router/index.test.ts:**
Import the router from `@/router` and test its configuration:

```typescript
import { describe, it, expect } from 'vitest'
import router from '@/router'
```

Use `router.getRoutes()` to get all registered routes. Assert:

- Route with path '/' exists and has name 'home'
- Route with path '/skills' exists and has name 'skills'
- Route with path '/cli' exists and has name 'cli'
- Route with path '/playground' exists and has name 'playground'
- Route with path '/playground/:remote' exists and has name 'playground-remote'
- A catch-all route exists with name 'not-found' (path may be normalized by vue-router, so check the name)
- `router.options.history` is web history (check via `router.options.history.base` or similar)

**App.test.ts:**

```typescript
import { describe, it, expect } from 'vitest'
import { shallowMount } from '@vue/test-utils'
import { createTestingPinia } from '@pinia/testing'
import App from '@/App.vue'
```

Mount with createTestingPinia and a router. Use shallowMount so AppLayout is stubbed. Verify:

1. Mount succeeds (no throw)
2. AppLayout stub is present in the output
3. CSS variables are applied (document.documentElement.style has --color-surface set) because useTheme() runs on setup
   </action>
   <verify>
   <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/router/**tests**/ apps/shell/src/**tests**/ --reporter=verbose 2>&1 | tail -20</automated>
   </verify>
   <acceptance_criteria> - `grep -q "getRoutes\|routes" apps/shell/src/router/__tests__/index.test.ts` returns 0 - `grep -q "home" apps/shell/src/router/__tests__/index.test.ts` returns 0 - `grep -q "skills" apps/shell/src/router/__tests__/index.test.ts` returns 0 - `grep -q "not-found" apps/shell/src/router/__tests__/index.test.ts` returns 0 - `grep -q "App" apps/shell/src/__tests__/App.test.ts` returns 0 - Both test files pass: exit code 0
   </acceptance_criteria>
   <done>Router configuration tested for all 6 route definitions with correct paths and names. App.vue tested for mount success, AppLayout composition, and useTheme initialization. All tests pass.</done>
   </task>

</tasks>

<verification>
- `bunx vitest run apps/shell/src/views/__tests__/ apps/shell/src/layouts/__tests__/ apps/shell/src/router/__tests__/ apps/shell/src/__tests__/ --reporter=verbose` -- all 8 test files pass
</verification>

<success_criteria>

- All 5 views have passing mount/render tests
- AppLayout tested for composition, conditional rendering, and container structure
- Router tested for all 6 route definitions
- App.vue tested for mount and useTheme initialization
- Zero test failures
  </success_criteria>

<output>
After completion, create `.planning/phases/999.8-unit-test-coverage/999.8-05-SUMMARY.md`
</output>
