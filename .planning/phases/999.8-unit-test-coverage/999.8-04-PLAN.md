---
phase: 999.8-unit-test-coverage
plan: 04
type: execute
wave: 3
depends_on: [999.8-02, 999.8-03]
files_modified:
  - apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts
  - apps/shell/src/components/skills/__tests__/DiamondRow.test.ts
  - apps/shell/src/components/skills/__tests__/DiamondWall.test.ts
  - apps/shell/src/components/skills/__tests__/DiamondInfoPanel.test.ts
  - apps/shell/src/components/skills/__tests__/SkillsToolbar.test.ts
  - apps/shell/src/components/skills/__tests__/CategoryPills.test.ts
  - apps/shell/src/components/skills/__tests__/ProficiencyToggle.test.ts
autonomous: true
requirements:
  - COV-COMPONENTS-SHELL
must_haves:
  truths:
    - 'All 7 skills components have unit tests covering rendering and interaction'
    - 'SkillDiamond renders skill icon and responds to hover events'
    - 'DiamondWall renders a grid of DiamondRow components'
    - 'SkillsToolbar contains CategoryPills and ProficiencyToggle'
    - 'CategoryPills renders pill buttons for each category and emits toggle events'
    - 'ProficiencyToggle renders mode options and emits selection events'
    - 'DiamondInfoPanel displays skill details'
  artifacts:
    - path: 'apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts'
      provides: 'SkillDiamond component tests'
      contains: 'SkillDiamond'
    - path: 'apps/shell/src/components/skills/__tests__/DiamondWall.test.ts'
      provides: 'DiamondWall component tests'
      contains: 'DiamondWall'
    - path: 'apps/shell/src/components/skills/__tests__/SkillsToolbar.test.ts'
      provides: 'SkillsToolbar component tests'
      contains: 'SkillsToolbar'
  key_links:
    - from: 'apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts'
      to: 'apps/shell/src/components/skills/SkillDiamond.vue'
      via: 'mount with createTestingPinia'
      pattern: 'mount.*SkillDiamond'
---

<objective>
Write unit tests for all 7 skills components in apps/shell/src/components/skills/. These are the most complex component tests due to store dependencies (skills store, theme store) and DOM interactions (hover, click, keyboard).

Purpose: Cover the diamond wall feature's component layer -- the most interactive and visually complex part of the application.
Output: 7 test files covering all skills components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
@.planning/phases/999.8-unit-test-coverage/999.8-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test SkillDiamond, DiamondInfoPanel, and DiamondRow</name>
  <files>
    apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts,
    apps/shell/src/components/skills/__tests__/DiamondInfoPanel.test.ts,
    apps/shell/src/components/skills/__tests__/DiamondRow.test.ts
  </files>
  <read_first>
    apps/shell/src/components/skills/SkillDiamond.vue,
    apps/shell/src/components/skills/DiamondInfoPanel.vue,
    apps/shell/src/components/skills/DiamondRow.vue,
    apps/shell/src/stores/skills.ts,
    apps/shell/src/types/skills.ts,
    apps/shell/src/data/techSkills.json
  </read_first>
  <behavior>
    SkillDiamond.test.ts:
    - Renders with the skill's icon (img src contains iconPath or aria-label contains displayName)
    - Applies full opacity class when skill is visible (All category active by default)
    - Emits hover event on mouseenter with skill data
    - Emits hover-end event on mouseleave
    - Renders in the correct proficiency mode visual (test at least 'uniform' mode)

    DiamondInfoPanel.test.ts:
    - Renders skill displayName when skill prop is provided
    - Renders skill category
    - Renders skill years
    - Does not render (or is hidden) when skill prop is null/undefined

    DiamondRow.test.ts:
    - Renders multiple SkillDiamond children based on skills prop
    - Passes correct props to each SkillDiamond child
    - Applies row-level styling (transform/rotation classes)

  </behavior>
  <action>
Create `apps/shell/src/components/skills/__tests__/` directory.

All component tests use this base pattern:

```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import { createTestingPinia } from '@pinia/testing'
```

**For each component:** Read the source .vue file FIRST to understand the exact props interface, emitted events, template structure, and store dependencies. Write tests based on what the component actually does, not assumptions.

Create a shared test skill fixture:

```typescript
const testSkill = {
  name: 'vue',
  displayName: 'Vue.js',
  iconPath: '/icons/vue.svg',
  category: 'Frontend',
  years: 3,
}
```

**SkillDiamond.test.ts:** Mount with `props: { skill: testSkill, mode: 'uniform', diamondSize: 80 }` and `global: { plugins: [createTestingPinia()] }`. Test rendering (check for skill name in aria-label or alt text), hover events (trigger mouseenter/mouseleave), and opacity based on store's isSkillVisible.

**DiamondInfoPanel.test.ts:** Read the component source to determine its exact props. Mount with the skill data and verify text content. Test the null/empty case.

**DiamondRow.test.ts:** Mount with an array of skills. Use `shallowMount` to avoid deep-rendering SkillDiamond children. Verify the correct number of child stubs are rendered.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/components/skills/**tests**/SkillDiamond.test.ts apps/shell/src/components/skills/**tests**/DiamondInfoPanel.test.ts apps/shell/src/components/skills/**tests**/DiamondRow.test.ts --reporter=verbose 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "SkillDiamond" apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts` returns 0 - `grep -q "createTestingPinia" apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts` returns 0 - `grep -q "mouseenter\|hover" apps/shell/src/components/skills/__tests__/SkillDiamond.test.ts` returns 0 - `grep -q "DiamondInfoPanel" apps/shell/src/components/skills/__tests__/DiamondInfoPanel.test.ts` returns 0 - `grep -q "DiamondRow" apps/shell/src/components/skills/__tests__/DiamondRow.test.ts` returns 0 - All 3 test files pass: exit code 0
</acceptance_criteria>
<done>SkillDiamond tested for rendering, hover events, and opacity. DiamondInfoPanel tested for skill detail display and null state. DiamondRow tested for child component rendering. All tests pass with createTestingPinia.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test DiamondWall, SkillsToolbar, CategoryPills, ProficiencyToggle</name>
  <files>
    apps/shell/src/components/skills/__tests__/DiamondWall.test.ts,
    apps/shell/src/components/skills/__tests__/SkillsToolbar.test.ts,
    apps/shell/src/components/skills/__tests__/CategoryPills.test.ts,
    apps/shell/src/components/skills/__tests__/ProficiencyToggle.test.ts
  </files>
  <read_first>
    apps/shell/src/components/skills/DiamondWall.vue,
    apps/shell/src/components/skills/SkillsToolbar.vue,
    apps/shell/src/components/skills/CategoryPills.vue,
    apps/shell/src/components/skills/ProficiencyToggle.vue,
    apps/shell/src/stores/skills.ts,
    apps/shell/src/types/skills.ts
  </read_first>
  <behavior>
    DiamondWall.test.ts:
    - Renders DiamondRow children (use shallowMount)
    - Renders the DiamondInfoPanel (or its stub)
    - Applies animation/scroll container styling

    SkillsToolbar.test.ts:
    - Renders CategoryPills child component
    - Renders ProficiencyToggle child component
    - Contains a search input element

    CategoryPills.test.ts:
    - Renders an "All" pill button
    - Renders pill buttons for each category from the skills store
    - Clicking a pill calls store.toggleCategory with the category name
    - "All" pill has active styling when activeCategories contains 'All'

    ProficiencyToggle.test.ts:
    - Renders buttons for each proficiency mode (uniform, glow, size, fill)
    - Clicking a mode button calls store.setProficiencyMode with the mode id
    - Active mode has distinct styling

  </behavior>
  <action>
**For each component:** Read the source .vue file FIRST to understand exact structure.

**DiamondWall.test.ts:** Use `shallowMount` to avoid rendering the full component tree. Mount with `global: { plugins: [createTestingPinia()] }`. Verify that DiamondRow stubs appear in the output. Check for the info panel element.

**SkillsToolbar.test.ts:** Use `shallowMount`. Verify CategoryPills and ProficiencyToggle stubs are rendered. Check for input[type="text"] or search input element.

**CategoryPills.test.ts:** Use `mount` (not shallow -- we need to test button clicks). Mount with `createTestingPinia()`. Verify rendered buttons include "All" and category names. Trigger click on a category button and assert that the skills store's `toggleCategory` was called. Access the store in the test via `useSkillsStore()` after createTestingPinia (pinia/testing auto-stubs actions so you can check `.toHaveBeenCalledWith()`).

**ProficiencyToggle.test.ts:** Similar to CategoryPills. Mount with createTestingPinia. Verify mode buttons render with correct labels ('Uniform', 'Glow', 'Size', 'Fill'). Click a button and assert `setProficiencyMode` was called.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/components/skills/**tests**/DiamondWall.test.ts apps/shell/src/components/skills/**tests**/SkillsToolbar.test.ts apps/shell/src/components/skills/**tests**/CategoryPills.test.ts apps/shell/src/components/skills/**tests**/ProficiencyToggle.test.ts --reporter=verbose 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "DiamondWall" apps/shell/src/components/skills/__tests__/DiamondWall.test.ts` returns 0 - `grep -q "SkillsToolbar" apps/shell/src/components/skills/__tests__/SkillsToolbar.test.ts` returns 0 - `grep -q "toggleCategory" apps/shell/src/components/skills/__tests__/CategoryPills.test.ts` returns 0 - `grep -q "setProficiencyMode" apps/shell/src/components/skills/__tests__/ProficiencyToggle.test.ts` returns 0 - All 4 test files pass: exit code 0
</acceptance_criteria>
<done>DiamondWall tested for row rendering and info panel presence. SkillsToolbar tested for child component composition. CategoryPills tested for button rendering and store interaction. ProficiencyToggle tested for mode rendering and store interaction. All tests pass.</done>
</task>

</tasks>

<verification>
- `bunx vitest run apps/shell/src/components/skills/__tests__/ --reporter=verbose` -- all 7 test files pass
</verification>

<success_criteria>

- All 7 skills components have passing unit tests
- Store interactions verified via createTestingPinia
- Hover events, click events, and rendering behavior covered
- Zero test failures
  </success_criteria>

<output>
After completion, create `.planning/phases/999.8-unit-test-coverage/999.8-04-SUMMARY.md`
</output>
