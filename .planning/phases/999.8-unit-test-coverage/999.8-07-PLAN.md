---
phase: 999.8-unit-test-coverage
plan: 07
type: execute
wave: 4
depends_on: [999.8-04, 999.8-05, 999.8-06]
files_modified:
  - apps/shell/vitest.config.ts
  - packages/ui/vitest.config.ts
autonomous: false
requirements:
  - COV-100
  - COV-CI
must_haves:
  truths:
    - 'Full test suite passes with zero failures across both workspace projects'
    - 'Coverage report shows 100% lines, functions, branches, statements (or thresholds are adjusted for truly uncoverable template branches with documented justification)'
    - 'bun run test:coverage exits 0 from project root'
  artifacts:
    - path: 'apps/shell/vitest.config.ts'
      provides: 'Final coverage config with any necessary exclude adjustments'
      contains: 'thresholds'
    - path: 'packages/ui/vitest.config.ts'
      provides: 'Final coverage config with any necessary exclude adjustments'
      contains: 'thresholds'
  key_links:
    - from: 'package.json'
      to: 'vitest.config.ts'
      via: 'test:coverage script runs vitest with --coverage flag'
      pattern: 'test:coverage.*vitest.*coverage'
---

<objective>
Run the full test suite with coverage reporting across both workspace projects. Identify and fix any coverage gaps. Adjust coverage excludes if there are truly uncoverable template branches. Verify the CI gate works.

Purpose: Close the loop -- ensure 100% coverage threshold is achievable and the CI gate will pass. This is the final quality verification before the phase is complete.
Output: Passing full test suite with 100% coverage (or documented threshold adjustments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
@.planning/phases/999.8-unit-test-coverage/999.8-01-SUMMARY.md
@.planning/phases/999.8-unit-test-coverage/999.8-02-SUMMARY.md
@.planning/phases/999.8-unit-test-coverage/999.8-03-SUMMARY.md
@.planning/phases/999.8-unit-test-coverage/999.8-04-SUMMARY.md
@.planning/phases/999.8-unit-test-coverage/999.8-05-SUMMARY.md
@.planning/phases/999.8-unit-test-coverage/999.8-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full coverage and fix gaps</name>
  <files>
    apps/shell/vitest.config.ts,
    packages/ui/vitest.config.ts
  </files>
  <read_first>
    apps/shell/vitest.config.ts,
    packages/ui/vitest.config.ts,
    .planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
  </read_first>
  <action>
1. Run the full test suite with coverage from the project root:
   ```bash
   bun run test:coverage
   ```

2. Analyze the coverage output. The coverage report will show lines, functions, branches, and statements percentages for each file.

3. **If any file is below 100%:**
   a. Check if the uncovered lines are in truly uncoverable code (e.g., compiled Vue template branches from slot fallbacks, dynamic component resolution, or unreachable default cases).
   b. If uncoverable: Add the specific file or pattern to the `coverage.exclude` array in the appropriate vitest.config.ts. Document why in a code comment.
   c. If coverable: Write additional test cases to cover the missing lines/branches. Add them to the existing test files from Plans 02-06.

4. **If coverage thresholds cannot reach 100% for specific categories:**
   Use per-file threshold overrides (NOT lowering the global threshold):

   ```typescript
   coverage: {
     thresholds: {
       lines: 100,
       functions: 100,
       branches: 100,
       statements: 100,
       // Per-file overrides for truly uncoverable template branches
       'src/components/skills/DiamondWall.vue': {
         branches: 95,  // v-if compiled branch in template slot fallback
       },
     },
   }
   ```

5. Re-run `bun run test:coverage` and confirm it exits with code 0.

6. Run `bun run test` (without coverage) to verify the non-coverage run also passes cleanly.

Per RESEARCH.md Open Question 1: "Start with 100% threshold. If specific files have truly uncoverable branches, use per-file threshold overrides rather than lowering the global threshold."
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun run test:coverage 2>&1 | tail -40</automated>
</verify>
<acceptance_criteria> - `bun run test:coverage` exits with code 0 - `bun run test` exits with code 0 - Coverage output shows all files meeting thresholds (no "ERROR: Coverage" lines) - If any per-file overrides were added, each has a comment explaining why
</acceptance_criteria>
<done>Full test suite passes with coverage meeting configured thresholds. Any truly uncoverable branches have documented per-file overrides. bun run test:coverage exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify test quality and coverage report</name>
  <files>apps/shell/vitest.config.ts, packages/ui/vitest.config.ts</files>
  <action>
Human verification checkpoint. Present the test results and coverage report to the user for review.

What was built: Complete unit test infrastructure and 100% coverage across the monorepo. Test suite covers 3 Pinia stores, 2 composables, 1 utility, 9 theme definitions, 13 Vue components, 5 views, 1 layout, 1 router config, 1 barrel export, and 1 root App component.

How to verify:

1. Run `bun run test` from project root -- should show all tests passing with zero failures
2. Run `bun run test:coverage` from project root -- should show coverage report with all thresholds met
3. Review any per-file threshold overrides in vitest configs -- do they have documented justifications?
4. Spot-check a test file (e.g., `apps/shell/src/stores/__tests__/skills.test.ts`) to verify test quality

Resume signal: Type "approved" or describe issues.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun run test 2>&1 | tail -5</automated>
</verify>
<done>User has verified test quality and coverage report. Phase 999.8 is complete.</done>
</task>

</tasks>

<verification>
- `bun run test` passes with zero failures
- `bun run test:coverage` passes with all thresholds met
- No unexplained coverage gaps
</verification>

<success_criteria>

- Full test suite green across both workspace projects
- Coverage thresholds enforced (100% global, with documented per-file overrides if needed)
- CI gate will block deploys on test failure or coverage regression
- User has verified test quality
  </success_criteria>

<output>
After completion, create `.planning/phases/999.8-unit-test-coverage/999.8-07-SUMMARY.md`
</output>
