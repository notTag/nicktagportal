---
phase: 999.8-unit-test-coverage
plan: 06
type: execute
wave: 3
depends_on: [999.8-01]
files_modified:
  - packages/ui/src/components/__tests__/TheHeader.test.ts
  - packages/ui/src/components/__tests__/TheFooter.test.ts
  - packages/ui/src/components/__tests__/SocialLinks.test.ts
  - packages/ui/src/components/__tests__/TerminalPanel.test.ts
  - packages/ui/src/components/__tests__/ThemeDropdown.test.ts
  - packages/ui/src/components/__tests__/MobileMenu.test.ts
  - packages/ui/src/__tests__/index.test.ts
autonomous: true
requirements:
  - COV-COMPONENTS-UI
must_haves:
  truths:
    - 'All 6 packages/ui components have unit tests covering rendering'
    - 'TheHeader renders navigation links and theme picker toggle'
    - 'TheFooter renders footer content with slot'
    - 'SocialLinks renders link items from props'
    - 'ThemeDropdown renders theme options from store'
    - 'MobileMenu renders overlay when open prop is true'
    - 'packages/ui barrel export re-exports all 6 components'
  artifacts:
    - path: 'packages/ui/src/components/__tests__/TheHeader.test.ts'
      provides: 'TheHeader component tests'
      contains: 'TheHeader'
    - path: 'packages/ui/src/components/__tests__/ThemeDropdown.test.ts'
      provides: 'ThemeDropdown component tests'
      contains: 'ThemeDropdown'
    - path: 'packages/ui/src/components/__tests__/MobileMenu.test.ts'
      provides: 'MobileMenu component tests'
      contains: 'MobileMenu'
    - path: 'packages/ui/src/__tests__/index.test.ts'
      provides: 'Barrel export tests'
      contains: 'TheHeader'
  key_links:
    - from: 'packages/ui/src/components/__tests__/ThemeDropdown.test.ts'
      to: 'packages/ui/src/components/ThemeDropdown.vue'
      via: 'mount with createTestingPinia'
      pattern: 'mount.*ThemeDropdown'
    - from: 'packages/ui/src/__tests__/index.test.ts'
      to: 'packages/ui/src/index.ts'
      via: 'import all named exports'
      pattern: 'TheHeader.*TheFooter'
---

<objective>
Write unit tests for all 6 packages/ui components and the barrel export file. These components import from @/stores/* (shell app paths) so the vitest config alias must work correctly.

Purpose: Cover the shared UI component library. This is the second workspace project in the monorepo and validates the cross-package alias resolution.
Output: 7 test files covering all UI components and barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
@.planning/phases/999.8-unit-test-coverage/999.8-01-SUMMARY.md

<interfaces>
From packages/ui/src/index.ts:
- Re-exports: TheHeader, TheFooter, SocialLinks, TerminalPanel, ThemeDropdown, MobileMenu

From packages/ui/src/components/:

- TheHeader.vue: props { showThemePicker: boolean, isMobileMenuOpen: boolean }, emits ['toggle-menu']
- TheFooter.vue: has default slot for content
- SocialLinks.vue: props { links: array, orientation: string }
- TerminalPanel.vue: (read source to determine props/behavior)
- ThemeDropdown.vue: uses useThemeStore from @/stores/theme, renders theme list
- MobileMenu.vue: props { isOpen: boolean }, emits ['close'], uses useThemeStore
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test TheHeader, TheFooter, SocialLinks, TerminalPanel</name>
  <files>
    packages/ui/src/components/__tests__/TheHeader.test.ts,
    packages/ui/src/components/__tests__/TheFooter.test.ts,
    packages/ui/src/components/__tests__/SocialLinks.test.ts,
    packages/ui/src/components/__tests__/TerminalPanel.test.ts
  </files>
  <read_first>
    packages/ui/src/components/TheHeader.vue,
    packages/ui/src/components/TheFooter.vue,
    packages/ui/src/components/SocialLinks.vue,
    packages/ui/src/components/TerminalPanel.vue,
    packages/ui/vitest.config.ts
  </read_first>
  <behavior>
    TheHeader.test.ts:
    - Renders navigation links (Home, Skills, CLI at minimum)
    - Renders theme picker area when showThemePicker prop is true
    - Hides theme picker area when showThemePicker prop is false
    - Emits 'toggle-menu' when hamburger/menu button is clicked
    - Renders a header element or nav element

    TheFooter.test.ts:
    - Renders without errors
    - Renders slot content when provided
    - Contains a footer element

    SocialLinks.test.ts:
    - Renders link items from links prop
    - Each link has correct href attribute
    - Renders nothing or empty when links array is empty

    TerminalPanel.test.ts:
    - Renders without errors
    - (Read source to determine specific behavior to test)

  </behavior>
  <action>
Create `packages/ui/src/components/__tests__/` directory.

IMPORTANT: These tests run in the packages/ui vitest project, which has `@` aliased to `../../apps/shell/src`. Components that import from `@/stores/theme` or `@/themes` will resolve correctly IF the vitest config is set up properly (done in Plan 01).

All component tests use:

```typescript
import { describe, it, expect } from 'vitest'
import { mount, shallowMount } from '@vue/test-utils'
import { createTestingPinia } from '@pinia/testing'
import { createRouter, createMemoryHistory } from 'vue-router'
```

Provide a router for components that use `<router-link>`:

```typescript
const router = createRouter({
  history: createMemoryHistory(),
  routes: [
    { path: '/', name: 'home', component: { template: '<div />' } },
    { path: '/skills', name: 'skills', component: { template: '<div />' } },
    { path: '/cli', name: 'cli', component: { template: '<div />' } },
  ],
})
```

**TheHeader.test.ts:** Read the source to find exact nav link text/structure. Mount with `props: { showThemePicker: true, isMobileMenuOpen: false }`, `global: { plugins: [createTestingPinia(), router] }`. Test navigation link presence, theme picker conditional rendering, and toggle-menu emit.

**TheFooter.test.ts:** Mount with slot content: `slots: { default: '<span>Test Footer</span>' }`. Verify slot content renders. Check for `<footer>` element.

**SocialLinks.test.ts:** Read source for props structure. Mount with sample links data: `props: { links: [{ url: 'https://github.com', icon: 'github', label: 'GitHub' }], orientation: 'left' }`. Verify link renders with href.

**TerminalPanel.test.ts:** Read source first. Mount accordingly and test basic rendering.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run packages/ui/src/components/**tests**/TheHeader.test.ts packages/ui/src/components/**tests**/TheFooter.test.ts packages/ui/src/components/**tests**/SocialLinks.test.ts packages/ui/src/components/**tests**/TerminalPanel.test.ts --reporter=verbose 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "TheHeader" packages/ui/src/components/__tests__/TheHeader.test.ts` returns 0 - `grep -q "showThemePicker" packages/ui/src/components/__tests__/TheHeader.test.ts` returns 0 - `grep -q "toggle-menu\|toggleMenu" packages/ui/src/components/__tests__/TheHeader.test.ts` returns 0 - `grep -q "TheFooter" packages/ui/src/components/__tests__/TheFooter.test.ts` returns 0 - `grep -q "slot\|default" packages/ui/src/components/__tests__/TheFooter.test.ts` returns 0 - `grep -q "SocialLinks" packages/ui/src/components/__tests__/SocialLinks.test.ts` returns 0 - `grep -q "TerminalPanel" packages/ui/src/components/__tests__/TerminalPanel.test.ts` returns 0 - All 4 test files pass: exit code 0
</acceptance_criteria>
<done>TheHeader tested for navigation, theme picker toggle, and menu emit. TheFooter tested for slot rendering. SocialLinks tested for link rendering from props. TerminalPanel tested for basic rendering. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test ThemeDropdown, MobileMenu, and barrel export</name>
  <files>
    packages/ui/src/components/__tests__/ThemeDropdown.test.ts,
    packages/ui/src/components/__tests__/MobileMenu.test.ts,
    packages/ui/src/__tests__/index.test.ts
  </files>
  <read_first>
    packages/ui/src/components/ThemeDropdown.vue,
    packages/ui/src/components/MobileMenu.vue,
    packages/ui/src/index.ts
  </read_first>
  <behavior>
    ThemeDropdown.test.ts:
    - Renders theme options from the themeList
    - Highlights the currently active theme
    - Calls store.setTheme when a theme is selected
    - Calls store.previewTheme on hover/focus of a theme option
    - Calls store.revertPreview on blur/leave
    - Has WAI-ARIA listbox role attributes

    MobileMenu.test.ts:
    - Renders overlay when isOpen prop is true
    - Does not render (or is hidden) when isOpen prop is false
    - Contains navigation links
    - Emits 'close' event when close button is clicked
    - Emits 'close' when a navigation link is clicked

    index.test.ts (barrel):
    - Exports TheHeader as a component
    - Exports TheFooter as a component
    - Exports SocialLinks as a component
    - Exports TerminalPanel as a component
    - Exports ThemeDropdown as a component
    - Exports MobileMenu as a component
    - Exactly 6 named exports

  </behavior>
  <action>
**ThemeDropdown.test.ts:** This component uses `useThemeStore` from `@/stores/theme` and imports `themeList` from `@/themes`. Mount with `createTestingPinia()`. Read the source to find exact DOM structure (listbox role, option role, etc.). Test:
1. Theme options are rendered (check for theme names like "SynthWave '84", "Dark Modern")
2. Active theme is highlighted (check for aria-selected or active class)
3. Clicking a theme option calls `setTheme` on the store
4. Hovering calls `previewTheme`, leaving calls `revertPreview`

**MobileMenu.test.ts:** Uses `useThemeStore`. Mount with `props: { isOpen: true }` and createTestingPinia + router. Test visibility based on isOpen prop. Test close emit on button click and link click.

**index.test.ts:** Simple import validation:

```typescript
import { describe, it, expect } from 'vitest'
import * as UIExports from '@ui/index'

describe('packages/ui barrel export', () => {
  it('exports all 6 components', () => {
    expect(UIExports.TheHeader).toBeDefined()
    expect(UIExports.TheFooter).toBeDefined()
    expect(UIExports.SocialLinks).toBeDefined()
    expect(UIExports.TerminalPanel).toBeDefined()
    expect(UIExports.ThemeDropdown).toBeDefined()
    expect(UIExports.MobileMenu).toBeDefined()
  })

  it('exports exactly 6 items', () => {
    expect(Object.keys(UIExports)).toHaveLength(6)
  })
})
```

Note: Use `@ui/index` or the relative path `../index` depending on what the vitest config aliases support. The packages/ui vitest.config.ts has `'@ui': resolve(__dirname, 'src')` so `@ui/index` should work, or import from `../index` relative to the test file location.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run packages/ui/src/components/**tests**/ThemeDropdown.test.ts packages/ui/src/components/**tests**/MobileMenu.test.ts packages/ui/src/**tests**/index.test.ts --reporter=verbose 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `grep -q "ThemeDropdown" packages/ui/src/components/__tests__/ThemeDropdown.test.ts` returns 0 - `grep -q "setTheme\|previewTheme" packages/ui/src/components/__tests__/ThemeDropdown.test.ts` returns 0 - `grep -q "MobileMenu" packages/ui/src/components/__tests__/MobileMenu.test.ts` returns 0 - `grep -q "isOpen" packages/ui/src/components/__tests__/MobileMenu.test.ts` returns 0 - `grep -q "TheHeader" packages/ui/src/__tests__/index.test.ts` returns 0 - `grep -q "MobileMenu" packages/ui/src/__tests__/index.test.ts` returns 0 - All 3 test files pass: exit code 0
</acceptance_criteria>
<done>ThemeDropdown tested for theme option rendering, selection, preview, and WAI-ARIA attributes. MobileMenu tested for open/close visibility, navigation links, and close emit. Barrel export verified for all 6 named exports. All tests pass.</done>
</task>

</tasks>

<verification>
- `bunx vitest run packages/ui/ --reporter=verbose` -- all 7 test files pass
- packages/ui vitest config correctly resolves @ alias to apps/shell/src
</verification>

<success_criteria>

- All 6 packages/ui components have passing unit tests
- Barrel export verified for completeness (6 named exports)
- Cross-package alias resolution works (@ -> apps/shell/src)
- Store interactions tested via createTestingPinia
- Zero test failures
  </success_criteria>

<output>
After completion, create `.planning/phases/999.8-unit-test-coverage/999.8-06-SUMMARY.md`
</output>
