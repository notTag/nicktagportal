---
phase: 999.8-unit-test-coverage
plan: 02
type: execute
wave: 2
depends_on: [999.8-01]
files_modified:
  - apps/shell/src/stores/__tests__/skills.test.ts
  - apps/shell/src/stores/__tests__/theme.test.ts
  - apps/shell/src/stores/__tests__/app.test.ts
  - apps/shell/src/utils/__tests__/debounce.test.ts
  - apps/shell/src/config/__tests__/features.test.ts
  - apps/shell/src/types/__tests__/skills.test.ts
  - apps/shell/src/federation/__tests__/remotes.test.ts
autonomous: true
requirements:
  - COV-STORES
  - COV-UTILS
  - COV-CONFIG
must_haves:
  truths:
    - 'All 3 Pinia stores have unit tests covering every exported action and getter'
    - 'debounce utility has tests covering delay behavior and cancellation'
    - 'Features config has tests verifying all feature flags exist and are typed'
    - 'Skills types has tests verifying PROFICIENCY_MODES constant values'
    - 'Federation remotes has tests covering resolveRemoteUrl in dev and prod modes'
  artifacts:
    - path: 'apps/shell/src/stores/__tests__/skills.test.ts'
      provides: 'Skills store unit tests'
      contains: 'useSkillsStore'
    - path: 'apps/shell/src/stores/__tests__/theme.test.ts'
      provides: 'Theme store unit tests'
      contains: 'useThemeStore'
    - path: 'apps/shell/src/stores/__tests__/app.test.ts'
      provides: 'App store unit tests'
      contains: 'useAppStore'
    - path: 'apps/shell/src/utils/__tests__/debounce.test.ts'
      provides: 'Debounce utility tests'
      contains: 'vi.useFakeTimers'
    - path: 'apps/shell/src/federation/__tests__/remotes.test.ts'
      provides: 'Federation remotes tests'
      contains: 'resolveRemoteUrl'
  key_links:
    - from: 'apps/shell/src/stores/__tests__/skills.test.ts'
      to: 'apps/shell/src/stores/skills.ts'
      via: 'import { useSkillsStore }'
      pattern: 'useSkillsStore'
    - from: 'apps/shell/src/stores/__tests__/theme.test.ts'
      to: 'apps/shell/src/stores/theme.ts'
      via: 'import { useThemeStore }'
      pattern: 'useThemeStore'
---

<objective>
Write unit tests for all pure logic modules: 3 Pinia stores, debounce utility, features config, skills types, and federation remotes. These are the highest-value targets (core business logic) and require no DOM.

Purpose: Cover all non-component, non-composable source files. Stores contain the application's core logic (category filtering, theme switching, proficiency modes).
Output: 7 test files, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-unit-test-coverage/999.8-RESEARCH.md
@.planning/phases/999.8-unit-test-coverage/999.8-01-SUMMARY.md

<interfaces>
From apps/shell/src/stores/skills.ts:
- exports: activeCategories (ref<Set<string>>), searchTerm (ref<string>), proficiencyMode (ref<ProficiencyMode>), allCategories (computed<string[]>), toggleCategory(category: string), setSearchTerm(term: string), setProficiencyMode(mode: ProficiencyMode), isSkillVisible(skillCategory: string, skillDisplayName: string): boolean

From apps/shell/src/stores/theme.ts:

- exports: themeId (ref<ThemeId>), previewingId (ref<ThemeId|null>), currentTheme (computed<Theme>), confirmedThemeId (computed<ThemeId>), activeThemeId (computed<ThemeId>), setTheme(id: ThemeId), previewTheme(id: ThemeId), revertPreview()
- reads localStorage key 'nicksite-theme' on creation
- DEFAULT_THEME_ID = 'synthwave-84'

From apps/shell/src/stores/app.ts:

- exports: isLoading (ref<boolean>), currentRemote (ref<string|null>), hasActiveRemote (computed<boolean>), setLoading(loading: boolean)

From apps/shell/src/utils/debounce.ts:

- exports: debounce<A>(fn: (...args: A) => void, delay: number): (...args: A) => void

From apps/shell/src/config/features.ts:

- exports: features = { showFooter: true, showThemePicker: true } as const
- exports: type FeatureFlags = typeof features

From apps/shell/src/types/skills.ts:

- exports: interface Skill { name, displayName, iconPath, category, years }
- exports: type ProficiencyMode = 'uniform' | 'glow' | 'size' | 'fill'
- exports: const PROFICIENCY_MODES: { id: ProficiencyMode; label: string }[]

From apps/shell/src/federation/remotes.ts:

- exports: type RemoteName = never
- exports: function resolveRemoteUrl(name: RemoteName): string
- Uses import.meta.env.DEV for environment detection
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Pinia stores (skills, theme, app)</name>
  <files>
    apps/shell/src/stores/__tests__/skills.test.ts,
    apps/shell/src/stores/__tests__/theme.test.ts,
    apps/shell/src/stores/__tests__/app.test.ts
  </files>
  <read_first>
    apps/shell/src/stores/skills.ts,
    apps/shell/src/stores/theme.ts,
    apps/shell/src/stores/app.ts,
    apps/shell/src/themes/index.ts,
    apps/shell/src/themes/types.ts,
    apps/shell/src/types/skills.ts,
    apps/shell/src/data/techSkills.json
  </read_first>
  <behavior>
    skills.test.ts:
    - Starts with activeCategories containing only 'All'
    - toggleCategory('Frontend') removes 'All' and adds 'Frontend'
    - toggleCategory('Frontend') again (deselect) falls back to 'All'
    - toggleCategory('All') resets to only 'All' regardless of current state
    - Toggling multiple categories keeps all of them active (no 'All')
    - allCategories starts with 'All' and includes sorted unique categories from techSkills.json
    - setSearchTerm('vue') sets searchTerm to 'vue'
    - setProficiencyMode('glow') sets proficiencyMode to 'glow'
    - isSkillVisible returns true when 'All' is active (any skill, any search empty)
    - isSkillVisible returns false when category doesn't match active categories
    - isSkillVisible returns false when search term doesn't match displayName
    - isSkillVisible returns true when both category and search match
    - isSkillVisible is case-insensitive for search

    theme.test.ts:
    - Starts with themeId 'synthwave-84' when localStorage is empty
    - Reads stored theme from localStorage if valid ThemeId
    - Falls back to 'synthwave-84' if localStorage has invalid value
    - setTheme('dark-modern') updates themeId and writes to localStorage
    - setTheme clears any active preview (previewingId becomes null)
    - previewTheme('red') sets previewingId to 'red'
    - currentTheme reflects preview when previewingId is set
    - currentTheme reflects confirmed theme when no preview
    - revertPreview() clears previewingId to null
    - activeThemeId returns previewingId when set, themeId otherwise
    - confirmedThemeId always returns themeId regardless of preview

    app.test.ts:
    - Starts with isLoading false
    - Starts with currentRemote null
    - hasActiveRemote is false when currentRemote is null
    - hasActiveRemote is true when currentRemote is set
    - setLoading(true) sets isLoading to true
    - setLoading(false) sets isLoading to false

  </behavior>
  <action>
Create `apps/shell/src/stores/__tests__/` directory and write three test files.

Each test file uses the pattern:

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
```

And calls `setActivePinia(createPinia())` in `beforeEach`.

**skills.test.ts:** Import `useSkillsStore` from `@/stores/skills`. Test all behaviors listed above. For `allCategories`, read the actual categories from `@/data/techSkills.json` to verify they match (do NOT hardcode category names -- import the JSON and derive expected categories).

**theme.test.ts:** Import `useThemeStore` from `@/stores/theme` and `DEFAULT_THEME_ID, themes` from `@/themes`. Note: localStorage.clear() is already called in beforeEach via test-setup.ts. For the "reads stored theme" test, call `localStorage.setItem('nicksite-theme', 'dark-modern')` BEFORE creating the store. For the "invalid value" test, set localStorage to `'nonexistent-theme'`.

**app.test.ts:** Import `useAppStore` from `@/stores/app`. Straightforward ref/computed testing.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/stores/**tests**/ --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `grep -q "useSkillsStore" apps/shell/src/stores/__tests__/skills.test.ts` returns 0 - `grep -q "toggleCategory" apps/shell/src/stores/__tests__/skills.test.ts` returns 0 - `grep -q "isSkillVisible" apps/shell/src/stores/__tests__/skills.test.ts` returns 0 - `grep -q "setActivePinia" apps/shell/src/stores/__tests__/skills.test.ts` returns 0 - `grep -q "useThemeStore" apps/shell/src/stores/__tests__/theme.test.ts` returns 0 - `grep -q "localStorage" apps/shell/src/stores/__tests__/theme.test.ts` returns 0 - `grep -q "previewTheme" apps/shell/src/stores/__tests__/theme.test.ts` returns 0 - `grep -q "useAppStore" apps/shell/src/stores/__tests__/app.test.ts` returns 0 - `grep -q "hasActiveRemote" apps/shell/src/stores/__tests__/app.test.ts` returns 0 - All 3 test files pass: `bunx vitest run apps/shell/src/stores/__tests__/` exits 0
</acceptance_criteria>
<done>All 3 Pinia stores have comprehensive unit tests. skills.test.ts covers toggleCategory (All toggle, individual toggle, fallback), allCategories, setSearchTerm, setProficiencyMode, and isSkillVisible (category match, search match, case insensitivity). theme.test.ts covers localStorage read/write, setTheme, previewTheme, revertPreview, and all computed properties. app.test.ts covers setLoading and hasActiveRemote. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test utilities, config, types, and federation</name>
  <files>
    apps/shell/src/utils/__tests__/debounce.test.ts,
    apps/shell/src/config/__tests__/features.test.ts,
    apps/shell/src/types/__tests__/skills.test.ts,
    apps/shell/src/federation/__tests__/remotes.test.ts
  </files>
  <read_first>
    apps/shell/src/utils/debounce.ts,
    apps/shell/src/config/features.ts,
    apps/shell/src/types/skills.ts,
    apps/shell/src/federation/remotes.ts
  </read_first>
  <behavior>
    debounce.test.ts:
    - Does not call fn immediately
    - Calls fn after specified delay
    - Resets timer on subsequent calls (only last call fires)
    - Passes arguments through to fn
    - Multiple rapid calls result in only one fn invocation

    features.test.ts:
    - features.showFooter is true
    - features.showThemePicker is true
    - features object is readonly (as const)

    skills.test.ts (types):
    - PROFICIENCY_MODES has exactly 4 entries
    - PROFICIENCY_MODES contains ids: 'uniform', 'glow', 'size', 'fill'
    - Each mode entry has both id and label properties

    remotes.test.ts:
    - resolveRemoteUrl function is exported
    - DEV mode returns localhost URL with port
    - PROD mode returns nicktag.tech URL with path
    (Note: RemoteName is `never` so we test the function exists and the URL construction logic.
     Since no remotes are configured, test that the function can be called with a cast value
     and returns the expected URL pattern.)

  </behavior>
  <action>
Create `__tests__/` directories under utils, config, types, and federation. Write four test files.

**debounce.test.ts:**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { debounce } from '@/utils/debounce'
```

Use `vi.useFakeTimers()` in beforeEach, `vi.useRealTimers()` in afterEach. Test delay with `vi.advanceTimersByTime(delay)`. Test cancellation by calling debounced twice and verifying fn called once after full delay.

**features.test.ts:**

```typescript
import { describe, it, expect } from 'vitest'
import { features } from '@/config/features'
```

Simple assertions on property values and types.

**skills.test.ts (types):**

```typescript
import { describe, it, expect } from 'vitest'
import { PROFICIENCY_MODES } from '@/types/skills'
```

Assert length, ids, and that each entry has `id` and `label` string properties.

**remotes.test.ts:**

```typescript
import { describe, it, expect, vi } from 'vitest'
import { resolveRemoteUrl } from '@/federation/remotes'
```

Note: `import.meta.env.DEV` in the source module controls dev vs prod. In vitest with happy-dom, `import.meta.env.DEV` is `true` by default. Test the dev URL pattern. For prod URL, mock `import.meta.env.DEV` to `false` using `vi.stubEnv('DEV', false)` (if available) or by checking the function's behavior. Since RemoteName is `never`, cast a string to test: `resolveRemoteUrl('testApp' as never)`. The function will return a URL based on the maps (which are empty, so the port/path will be `undefined`). Verify the URL pattern structure: in DEV mode it should contain `localhost` and `/assets/remoteEntry.js`.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vitest run apps/shell/src/utils/**tests**/ apps/shell/src/config/**tests**/ apps/shell/src/types/**tests**/ apps/shell/src/federation/**tests**/ --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `grep -q "vi.useFakeTimers" apps/shell/src/utils/__tests__/debounce.test.ts` returns 0 - `grep -q "vi.advanceTimersByTime" apps/shell/src/utils/__tests__/debounce.test.ts` returns 0 - `grep -q "features.showFooter" apps/shell/src/config/__tests__/features.test.ts` returns 0 - `grep -q "PROFICIENCY_MODES" apps/shell/src/types/__tests__/skills.test.ts` returns 0 - `grep -q "resolveRemoteUrl" apps/shell/src/federation/__tests__/remotes.test.ts` returns 0 - All 4 test files pass: `bunx vitest run apps/shell/src/utils/__tests__/ apps/shell/src/config/__tests__/ apps/shell/src/types/__tests__/ apps/shell/src/federation/__tests__/` exits 0
</acceptance_criteria>
<done>debounce utility tested for delay, cancellation, argument passing. features config tested for expected flag values. PROFICIENCY_MODES tested for correct entries. resolveRemoteUrl tested for URL construction pattern. All tests pass.</done>
</task>

</tasks>

<verification>
- `bunx vitest run apps/shell/src/stores/__tests__/ apps/shell/src/utils/__tests__/ apps/shell/src/config/__tests__/ apps/shell/src/types/__tests__/ apps/shell/src/federation/__tests__/ --reporter=verbose` -- all tests pass
- 7 test files created total across 5 directories
</verification>

<success_criteria>

- All 3 Pinia stores have passing unit tests covering every action and getter
- debounce utility has passing tests covering timing behavior
- features, types, and federation modules each have passing tests
- Zero test failures when running the full suite
  </success_criteria>

<output>
After completion, create `.planning/phases/999.8-unit-test-coverage/999.8-02-SUMMARY.md`
</output>
