---
phase: 01-monorepo-and-shell-app-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - apps/shell/package.json
  - apps/shell/tsconfig.json
  - apps/shell/tsconfig.app.json
  - apps/shell/tsconfig.node.json
  - apps/shell/vite.config.ts
  - apps/shell/index.html
  - apps/shell/src/main.ts
  - apps/shell/src/App.vue
  - apps/shell/src/assets/main.css
  - apps/shell/src/router/index.ts
  - apps/shell/src/stores/app.ts
  - apps/shell/src/views/HomeView.vue
  - apps/shell/src/views/PlaygroundView.vue
  - apps/shell/src/views/NotFoundView.vue
  - packages/ui/package.json
  - packages/ui/src/index.ts
  - packages/types/package.json
  - packages/types/src/index.ts
autonomous: true
requirements:
  - MONO-01
  - MONO-02
  - MONO-03
  - MONO-04
  - MONO-05
  - MONO-06
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - SHELL-06
  - SHELL-07
  - SHELL-09

must_haves:
  truths:
    - 'bun install at repo root installs all workspace dependencies without errors'
    - 'bun run dev starts the Vite dev server and serves the shell app on localhost'
    - 'Navigating to / loads HomeView with visible Tailwind-styled text'
    - 'Navigating to /playground loads PlaygroundView via lazy import'
    - 'Navigating to a nonexistent path shows the NotFoundView 404 page'
    - 'bun run typecheck passes with zero errors including cross-package path aliases'
  artifacts:
    - path: 'package.json'
      provides: 'Bun workspace config and root scripts'
      contains: 'workspaces'
    - path: 'tsconfig.json'
      provides: 'Root TypeScript strict config with path aliases'
      contains: 'strict'
    - path: 'apps/shell/vite.config.ts'
      provides: 'Vite config with Vue, Tailwind, and path aliases'
      contains: '@tailwindcss/vite'
    - path: 'apps/shell/src/main.ts'
      provides: 'Vue 3 bootstrap with Router and Pinia'
      contains: 'createPinia'
    - path: 'apps/shell/src/router/index.ts'
      provides: 'Route definitions with eager, lazy, and catch-all routes'
      contains: 'createWebHistory'
    - path: 'packages/ui/src/index.ts'
      provides: 'Barrel export for UI components'
      min_lines: 1
    - path: 'packages/types/src/index.ts'
      provides: 'Type exports placeholder'
      min_lines: 1
  key_links:
    - from: 'apps/shell/src/main.ts'
      to: 'apps/shell/src/App.vue'
      via: 'createApp(App)'
      pattern: "createApp\\(App\\)"
    - from: 'apps/shell/src/main.ts'
      to: 'apps/shell/src/assets/main.css'
      via: 'CSS import'
      pattern: "import.*main\\.css"
    - from: 'apps/shell/vite.config.ts'
      to: '@tailwindcss/vite'
      via: 'Vite plugin'
      pattern: "tailwindcss\\(\\)"
    - from: 'tsconfig.json'
      to: 'packages/ui/src'
      via: 'path alias @ui'
      pattern: '@ui'
---

<objective>
Scaffold the complete Bun monorepo with apps/shell as a running Vue 3 SPA, including TypeScript strict mode, Vue Router with all route definitions, Pinia state management, TailwindCSS v4 CSS-first config, and both shared packages (ui, types) as workspace scaffolds. By the end of this plan, `bun run dev` serves a working app with styled views and `bun run typecheck` passes.

Purpose: Establish the entire project foundation that all future phases build on. Every config file, every path alias, every package boundary is set here.
Output: A functioning monorepo with a running shell app at localhost, styled placeholder views, and cross-package TypeScript resolution.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-and-shell-app-core/1-CONTEXT.md
@.planning/phases/01-monorepo-and-shell-app-core/01-RESEARCH.md
@.planning/phases/01-monorepo-and-shell-app-core/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root config and shared package scaffolds</name>
  <files>
    package.json,
    tsconfig.json,
    .gitignore,
    packages/ui/package.json,
    packages/ui/src/index.ts,
    packages/types/package.json,
    packages/types/src/index.ts
  </files>
  <read_first>
    CLAUDE.md (monorepo structure, path alias definitions, version pinning),
    .planning/phases/01-monorepo-and-shell-app-core/01-RESEARCH.md (Pattern 1: Bun Workspace Configuration, Pattern 2: Dual Path Alias Configuration)
  </read_first>
  <action>
**1. Create root `package.json`** (per MONO-01, MONO-02):
```json
{
  "name": "nick-site",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter './apps/shell' dev",
    "build": "bun run --filter './apps/shell' build",
    "build:all": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
}
```
Note: `lint` and `format` scripts are defined now but will not work until Plan 03 installs ESLint/Prettier. This is intentional -- scripts are part of MONO-02.

**2. Create root `tsconfig.json`** (per MONO-03, MONO-04):

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "paths": {
      "@/*": ["./apps/shell/src/*"],
      "@ui/*": ["./packages/ui/src/*"],
      "@ui": ["./packages/ui/src"],
      "@types/*": ["./packages/types/src/*"]
    }
  },
  "references": [
    { "path": "./apps/shell/tsconfig.app.json" },
    { "path": "./apps/shell/tsconfig.node.json" }
  ]
}
```

**3. Create `.gitignore`** (per SHELL-09 -- keep repo clean):

```
node_modules
dist
.env*
.DS_Store
*.local
```

**4. Create `packages/ui/package.json`** (per MONO-05, D-09):

```json
{
  "name": "@nick-site/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

**5. Create `packages/ui/src/index.ts`** (per D-10 barrel export pattern):
Start empty with a comment. TheHeader and TheFooter exports will be added in Plan 02.

```typescript
// Barrel exports for @nick-site/ui
// Components added in Plan 02
export {}
```

**6. Create `packages/types/package.json`** (per MONO-06):

```json
{
  "name": "@nick-site/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

**7. Create `packages/types/src/index.ts`** (per MONO-06):

```typescript
// Type exports for @nick-site/types
// Types added as needed
export {}
```

**IMPORTANT:** Do NOT create `packages/ui/README.md` or `packages/ui/CLAUDE.md` in this task. Those are created in Plan 02, Task 2 (per D-12). Do NOT create `packages/types/README.md` here either -- it will be added in Plan 02, Task 2.
</action>
<verify>
<automated>test -f package.json && test -f tsconfig.json && test -f .gitignore && test -f packages/ui/package.json && test -f packages/ui/src/index.ts && test -f packages/types/package.json && test -f packages/types/src/index.ts && echo "PASS: All root and package scaffold files exist"</automated>
</verify>
<acceptance_criteria> - package.json contains `"workspaces": ["apps/*", "packages/*"]` - package.json contains `"type": "module"` - package.json contains scripts: "dev", "build", "build:all", "typecheck", "lint", "format" - tsconfig.json contains `"strict": true` - tsconfig.json contains `"paths"` with `"@/*"`, `"@ui/*"`, `"@ui"`, `"@types/*"` - tsconfig.json contains `"moduleResolution": "bundler"` - .gitignore contains `node_modules`, `dist`, `.env*`, `.DS_Store` - packages/ui/package.json contains `"name": "@nick-site/ui"` and `"main": "src/index.ts"` - packages/types/package.json contains `"name": "@nick-site/types"` and `"main": "src/index.ts"` - packages/ui/src/index.ts exists with `export {}` - packages/types/src/index.ts exists with `export {}` - No `tailwind.config.js` file exists anywhere in the repo - No `.js` files exist in packages/ (all TypeScript per SHELL-09)
</acceptance_criteria>
<done>Root monorepo config established with workspaces, strict TypeScript, path aliases, and both shared packages scaffolded with valid package.json and barrel export index.ts files.</done>
</task>

<task type="auto">
  <name>Task 2: Create shell app with Vue 3, Router, Pinia, TailwindCSS v4, and all views</name>
  <files>
    apps/shell/package.json,
    apps/shell/tsconfig.json,
    apps/shell/tsconfig.app.json,
    apps/shell/tsconfig.node.json,
    apps/shell/vite.config.ts,
    apps/shell/index.html,
    apps/shell/src/main.ts,
    apps/shell/src/App.vue,
    apps/shell/src/assets/main.css,
    apps/shell/src/router/index.ts,
    apps/shell/src/stores/app.ts,
    apps/shell/src/views/HomeView.vue,
    apps/shell/src/views/PlaygroundView.vue,
    apps/shell/src/views/NotFoundView.vue,
    apps/shell/env.d.ts
  </files>
  <read_first>
    CLAUDE.md (technology stack table, module federation config pattern, TailwindCSS v4 pattern),
    .planning/phases/01-monorepo-and-shell-app-core/01-RESEARCH.md (all 7 patterns, all 7 pitfalls, code examples section),
    .planning/phases/01-monorepo-and-shell-app-core/01-UI-SPEC.md (all component visual contracts, color @theme block, typography, copywriting contract),
    .planning/phases/01-monorepo-and-shell-app-core/1-CONTEXT.md (decisions D-05, D-13, D-14, D-15),
    package.json (created in Task 1 -- verify workspace config),
    tsconfig.json (created in Task 1 -- verify path aliases)
  </read_first>
  <action>
**1. Create `apps/shell/package.json`** (per MONO-01 workspace, SHELL-01 Vue 3):

First, verify exact latest versions within CLAUDE.md ranges:

```bash
npm view vue-router@4 version && npm view pinia@2 version && npm view @vitejs/plugin-vue@5 version && npm view vue-tsc@2 version && npm view vite-plugin-vue-devtools@7 version
```

Then create package.json using the verified versions (use exact versions from npm view output, not ranges):

```json
{
  "name": "@nick-site/shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.5",
    "vue-router": "^4.5",
    "pinia": "^2.3",
    "@nick-site/ui": "workspace:*",
    "@nick-site/types": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0",
    "@vitejs/plugin-vue": "^5.2",
    "typescript": "^5.7",
    "vue-tsc": "^2.2",
    "tailwindcss": "^4.0",
    "@tailwindcss/vite": "^4.0",
    "vite-plugin-vue-devtools": "^7.0"
  }
}
```

Use caret ranges matching CLAUDE.md. Bun will resolve to latest within range at install time.

**2. Create `apps/shell/tsconfig.json`** (per MONO-03):

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

**3. Create `apps/shell/tsconfig.app.json`** (per MONO-03, MONO-04):

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@ui/*": ["../../packages/ui/src/*"],
      "@ui": ["../../packages/ui/src"],
      "@types/*": ["../../packages/types/src/*"]
    }
  },
  "include": [
    "env.d.ts",
    "src/**/*",
    "src/**/*.vue",
    "../../packages/ui/src/**/*",
    "../../packages/types/src/**/*"
  ]
}
```

**4. Create `apps/shell/tsconfig.node.json`** (for vite.config.ts compilation):

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["node"]
  },
  "include": ["vite.config.ts"]
}
```

**5. Create `apps/shell/env.d.ts`** (Vue SFC type declarations):

```typescript
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<
    Record<string, unknown>,
    Record<string, unknown>,
    unknown
  >
  export default component
}
```

**6. Create `apps/shell/vite.config.ts`** (per SHELL-07, MONO-04):

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'
import vueDevTools from 'vite-plugin-vue-devtools'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue(), tailwindcss(), vueDevTools()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
      '@ui': resolve(__dirname, '../../packages/ui/src'),
      '@types': resolve(__dirname, '../../packages/types/src'),
    },
  },
})
```

Do NOT add Module Federation plugin here -- that is Phase 2 (FED-01). Do NOT add `build.target: 'esnext'` -- that is also Phase 2 (FED-04).

**7. Create `apps/shell/index.html`**:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <title>nick-site</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

The Google Fonts link loads Inter with weights 400 and 700 per UI-SPEC typography contract.

**8. Create `apps/shell/src/assets/main.css`** (per SHELL-07, UI-SPEC Color section):

```css
@import 'tailwindcss';

@theme {
  --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
  --color-surface: #0f172a;
  --color-surface-raised: #1e293b;
  --color-accent: #3b82f6;
  --color-destructive: #ef4444;
  --color-text: #f1f5f9;
  --color-text-muted: #94a3b8;
  --color-border: #334155;
}
```

No `tailwind.config.js`. No PostCSS config. The `@tailwindcss/vite` plugin handles everything.

**9. Create `apps/shell/src/main.ts`** (per SHELL-01, SHELL-06, RESEARCH main.ts example):

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import './assets/main.css'

const app = createApp(App)

app.use(createPinia())
app.use(router)

app.mount('#app')
```

**10. Create `apps/shell/src/App.vue`** (per D-05 -- uses AppLayout, but since AppLayout imports from @ui which gets components in Plan 02, use a temporary simple layout):

IMPORTANT: Since TheHeader and TheFooter do not exist yet (they are created in Plan 02), App.vue in this plan must use a simple `<RouterView />` wrapper. The AppLayout component (which imports from @ui) will be created in Plan 02 when the UI components exist.

```vue
<script setup lang="ts"></script>

<template>
  <div class="bg-surface text-text flex min-h-screen flex-col">
    <main class="flex-1">
      <RouterView />
    </main>
  </div>
</template>
```

Plan 02 will replace this with AppLayout integration once TheHeader/TheFooter exist in packages/ui.

**11. Create `apps/shell/src/router/index.ts`** (per SHELL-02, SHELL-03, SHELL-04, SHELL-05, RESEARCH Pattern 4):

```typescript
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '../views/HomeView.vue'

const router = createRouter({
  history: createWebHistory('/'),
  routes: [
    {
      path: '/',
      name: 'home',
      component: HomeView,
    },
    {
      path: '/playground',
      name: 'playground',
      component: () => import('../views/PlaygroundView.vue'),
    },
    {
      path: '/playground/:remote',
      name: 'playground-remote',
      component: () => import('../views/PlaygroundView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      name: 'not-found',
      component: () => import('../views/NotFoundView.vue'),
    },
  ],
})

export default router
```

HomeView is eager-loaded (static import per SHELL-04). PlaygroundView and NotFoundView are lazy-loaded (dynamic import per SHELL-03, SHELL-05). Catch-all uses `/:pathMatch(.*)*` syntax (Vue Router 4 pattern, not the old `*`).

**12. Create `apps/shell/src/stores/app.ts`** (per SHELL-06, RESEARCH Pattern 5):

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useAppStore = defineStore('app', () => {
  const isLoading = ref(false)
  const currentRemote = ref<string | null>(null)

  const hasActiveRemote = computed(() => currentRemote.value !== null)

  function setLoading(loading: boolean) {
    isLoading.value = loading
  }

  return { isLoading, currentRemote, hasActiveRemote, setLoading }
})
```

Uses Composition API setup syntax (not options) per SHELL-06.

**13. Create `apps/shell/src/views/HomeView.vue`** (per D-13, UI-SPEC HomeView contract):

```vue
<script setup lang="ts"></script>

<template>
  <div class="mx-auto max-w-5xl px-4 py-16">
    <section class="text-center">
      <h1 class="text-text text-4xl font-bold">Nick</h1>
      <p class="text-text-muted mt-2 text-2xl font-bold">Technical Lead</p>
      <p class="text-text-muted mt-4 text-base">
        Portfolio and tech playground
      </p>
    </section>
  </div>
</template>
```

Exact copy from UI-SPEC copywriting contract. Phase 2 replaces this with polished content.

**14. Create `apps/shell/src/views/PlaygroundView.vue`** (per D-14, UI-SPEC PlaygroundView contract):

```vue
<script setup lang="ts">
import { useRoute } from 'vue-router'

const route = useRoute()
</script>

<template>
  <div class="mx-auto max-w-5xl px-4 py-16">
    <h1 class="text-text text-2xl font-bold">Playground</h1>
    <p class="text-text-muted mt-2 text-base">
      Micro-frontend experimentation zone
    </p>
    <div id="remote-mount" class="border-border mt-8 rounded-lg border p-8">
      <p class="text-text-muted text-center text-sm">No remotes loaded</p>
    </div>
    <p v-if="route.params.remote" class="text-text-muted mt-4 text-sm">
      Remote: {{ route.params.remote }}
    </p>
  </div>
</template>
```

Shows `:remote` route param if present per UI-SPEC interaction contract.

**15. Create `apps/shell/src/views/NotFoundView.vue`** (per D-15, UI-SPEC NotFoundView contract):

```vue
<script setup lang="ts"></script>

<template>
  <div class="mx-auto max-w-5xl px-4 py-16 text-center">
    <p class="text-text-muted text-6xl font-bold">404</p>
    <h1 class="text-text mt-4 text-2xl font-bold">Page not found</h1>
    <p class="text-text-muted mt-2 text-base">
      The page you're looking for doesn't exist or has been moved.
    </p>
    <RouterLink to="/" class="text-accent mt-6 inline-block hover:underline">
      Go back home
    </RouterLink>
  </div>
</template>
```

This is a real, usable 404 page per D-15 -- not a placeholder. Copy is exact from UI-SPEC.

**16. Install dependencies:**

```bash
cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun install
```

**17. Verify dev server starts:**

```bash
cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal/apps/shell && bunx vite --host 2>&1 | head -20
```

Confirm it starts without errors. Kill after confirming.

**18. Verify typecheck passes:**

```bash
cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun run typecheck
```

If `bun run --filter` has issues, fall back to:

```bash
cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal/apps/shell && bunx vue-tsc --noEmit
```

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun install && cd apps/shell && bunx vue-tsc --noEmit && echo "PASS: typecheck passes"</automated>
  </verify>
  <acceptance_criteria>
    - apps/shell/package.json contains `"vue": "^3.5"` in dependencies
    - apps/shell/package.json contains `"vue-router": "^4.5"` in dependencies (NOT v5)
    - apps/shell/package.json contains `"pinia": "^2.3"` in dependencies (NOT v3)
    - apps/shell/package.json contains `"@nick-site/ui": "workspace:*"` in dependencies
    - apps/shell/package.json contains `"@nick-site/types": "workspace:*"` in dependencies
    - apps/shell/package.json contains `"@tailwindcss/vite"` in devDependencies
    - apps/shell/vite.config.ts contains `tailwindcss()` as a plugin
    - apps/shell/vite.config.ts contains `resolve.alias` with `@`, `@ui`, `@types`
    - apps/shell/vite.config.ts does NOT contain `federation` or `@originjs` (Phase 2)
    - apps/shell/src/assets/main.css contains `@import "tailwindcss"`
    - apps/shell/src/assets/main.css contains `--color-surface: #0f172a`
    - apps/shell/src/assets/main.css contains `--font-sans: 'Inter'`
    - apps/shell/src/main.ts contains `createPinia()` and `app.use(router)`
    - apps/shell/src/main.ts contains `import './assets/main.css'`
    - apps/shell/src/router/index.ts contains `createWebHistory('/')`
    - apps/shell/src/router/index.ts contains `import HomeView from` (eager, static import)
    - apps/shell/src/router/index.ts contains `() => import('../views/PlaygroundView.vue')` (lazy)
    - apps/shell/src/router/index.ts contains `/:pathMatch(.*)*` (catch-all)
    - apps/shell/src/stores/app.ts contains `defineStore('app', () =>` (setup syntax)
    - apps/shell/src/views/HomeView.vue contains `<script setup lang="ts">`
    - apps/shell/src/views/HomeView.vue contains text "Nick" and "Technical Lead"
    - apps/shell/src/views/PlaygroundView.vue contains `id="remote-mount"`
    - apps/shell/src/views/PlaygroundView.vue contains `route.params.remote`
    - apps/shell/src/views/NotFoundView.vue contains "404" and "Page not found"
    - apps/shell/src/views/NotFoundView.vue contains `RouterLink to="/"`
    - apps/shell/index.html contains `fonts.googleapis.com` with `Inter:wght@400;700`
    - No `tailwind.config.js` file exists anywhere
    - No `.js` files exist in apps/shell/src/ (all .ts or .vue per SHELL-09)
    - `bun run typecheck` exits with code 0
    - `bun install` exits with code 0
  </acceptance_criteria>
  <done>Shell app runs via `bun run dev`, displays styled placeholder views at /, /playground, and 404 paths, and `bun run typecheck` passes with zero errors. All source files are TypeScript.</done>
</task>

</tasks>

<verification>
1. `bun install` at repo root completes without errors
2. `bun run typecheck` (or equivalent) passes with zero errors
3. `bun run dev` starts Vite dev server successfully
4. All views use `<script setup lang="ts">` (no Options API, no plain JS)
5. No `tailwind.config.js` exists anywhere in the repo
6. `packages/ui/src/index.ts` and `packages/types/src/index.ts` exist as barrel exports
7. Path aliases (@, @ui, @types) resolve in both tsconfig and Vite
</verification>

<success_criteria>

- The monorepo structure matches the architecture from RESEARCH.md
- `bun install` && `bun run typecheck` passes
- `bun run dev` starts and serves the app
- Navigating to `/`, `/playground`, `/playground/test`, and `/nonexistent` each render the correct view
- TailwindCSS v4 utility classes render correctly (visible in browser -- dark background, styled text)
- Zero `.js` files in src directories (SHELL-09)
- Zero Options API usage (SHELL-01)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-and-shell-app-core/01-01-SUMMARY.md`
</output>
