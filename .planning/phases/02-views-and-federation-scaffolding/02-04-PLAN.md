---
phase: 02-views-and-federation-scaffolding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shell/src/data/socialLinks.json
  - packages/ui/src/components/SocialLinks.vue
  - apps/shell/src/layouts/AppLayout.vue
autonomous: true
gap_closure: true
requirements: [VIEW-03]

must_haves:
  truths:
    - 'GitHub link navigates to https://github.com/notTag'
    - 'LinkedIn link navigates to https://www.linkedin.com/in/nicktagliasacchi/'
    - 'Email link is mailto:nick.tagliasacchi@gmail.com'
    - 'Phone link is tel:+16319026516'
    - 'Hovering a social link turns it pink/magenta (accent color)'
    - 'Footer text is readable (not text-sm)'
    - 'Footer is sticky at the bottom of the viewport and always visible'
  artifacts:
    - path: 'apps/shell/src/data/socialLinks.json'
      provides: 'Correct URLs for all 4 social links'
    - path: 'packages/ui/src/components/SocialLinks.vue'
      provides: 'Readable text size and working hover color'
    - path: 'apps/shell/src/layouts/AppLayout.vue'
      provides: 'Sticky footer positioning'
  key_links:
    - from: 'apps/shell/src/layouts/AppLayout.vue'
      to: 'packages/ui/src/components/SocialLinks.vue'
      via: 'SocialLinks component rendered in TheFooter slot'
      pattern: '<SocialLinks'
---

<objective>
Fix footer social links: correct URLs, working hover color, readable text size, and sticky positioning.

Purpose: UAT test 2 failed -- footer links have wrong destinations, hover state is broken, text is too small, and footer is not sticky.
Output: Correct, styled, sticky footer with functional social links.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-views-and-federation-scaffolding/02-01-SUMMARY.md

<interfaces>
<!-- Current source files the executor will modify -->

From apps/shell/src/data/socialLinks.json:

```json
{
  "orientation": "center",
  "links": [
    {
      "type": "github",
      "label": "GitHub",
      "url": "https://github.com/nicktag",
      "icon": "github"
    },
    {
      "type": "linkedin",
      "label": "LinkedIn",
      "url": "https://linkedin.com/in/nicktag",
      "icon": "linkedin"
    },
    {
      "type": "email",
      "label": "Email",
      "url": "mailto:nick@nicktag.tech",
      "icon": "email"
    },
    {
      "type": "phone",
      "label": "Phone",
      "url": "tel:+1XXXXXXXXXX",
      "icon": "phone"
    }
  ]
}
```

From packages/ui/src/components/SocialLinks.vue:

```vue
<!-- Uses text-sm and hover:text-accent classes -->
<a class="text-text-muted hover:text-accent text-sm transition-colors">
```

From apps/shell/src/layouts/AppLayout.vue:

```vue
<div class="bg-surface text-text flex min-h-screen flex-col">
  <TheHeader />
  <main class="flex-1"><RouterView /></main>
  <TheFooter v-if="features.showFooter">
    <SocialLinks :links="socialLinksData.links" :orientation="..." />
  </TheFooter>
</div>
```

Design tokens from main.css:

- --color-accent: #ff7edb (pink/magenta)
- --color-text-muted: #848bbd
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix social link URLs, text size, hover color, and sticky footer</name>
  <files>apps/shell/src/data/socialLinks.json, packages/ui/src/components/SocialLinks.vue, apps/shell/src/layouts/AppLayout.vue</files>
  <action>
    1. Update apps/shell/src/data/socialLinks.json with correct URLs:
       - GitHub: "https://github.com/notTag"
       - LinkedIn: "https://www.linkedin.com/in/nicktagliasacchi/"
       - Email: "mailto:nick.tagliasacchi@gmail.com"
       - Phone: "tel:+16319026516"

    2. In packages/ui/src/components/SocialLinks.vue, change the link anchor class from `text-sm` to `text-base` for readable size. The hover class `hover:text-accent` should work if Tailwind scans the ui package files. Verify the @tailwindcss/vite plugin picks up packages/ui by checking if the resolved accent color renders. If hover does NOT work, the issue is that Tailwind v4 auto-detects source files but may not reach outside the app root. In that case, add a `@source` directive in apps/shell/src/assets/main.css:
       ```css
       @source "../../../packages/ui/src/**/*.vue";
       ```
       This tells Tailwind v4 to also scan the ui package for class usage.

    3. In apps/shell/src/layouts/AppLayout.vue, make the footer sticky at the bottom. The current layout uses `min-h-screen flex flex-col` with `main flex-1`, which pushes the footer to the bottom of content. To make it always visible (sticky), change the outer div to use `h-screen` instead of `min-h-screen`, and make main scrollable with `overflow-y-auto`. This keeps the footer pinned at the viewport bottom while main content scrolls independently:
       ```
       <div class="bg-surface text-text flex h-screen flex-col">
         <TheHeader />
         <main class="flex-1 overflow-y-auto">
           <RouterView />
         </main>
         <TheFooter ...>
       ```

    4. Run `bun run dev` in apps/shell and verify:
       - Footer is always visible at viewport bottom
       - Hovering links turns them pink (#ff7edb)
       - Link URLs are correct in the DOM (inspect with dev tools or check the JSON)

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bunx vue-tsc --noEmit --project apps/shell/tsconfig.app.json</automated>
  </verify>
  <done>All 4 social links have correct URLs. Hover turns links pink. Footer text is text-base (readable). Footer is sticky at viewport bottom. TypeScript passes.</done>
</task>

</tasks>

<verification>
- Open http://localhost:5173 and confirm footer is always visible at the bottom of the viewport
- Hover each social link and confirm pink/magenta color change
- Click GitHub link and verify it navigates to https://github.com/notTag
- Click LinkedIn link and verify it navigates to https://www.linkedin.com/in/nicktagliasacchi/
- Inspect email link href: mailto:nick.tagliasacchi@gmail.com
- Inspect phone link href: tel:+16319026516
</verification>

<success_criteria>
UAT test 2 passes: Footer shows GitHub, LinkedIn, Email, Phone with correct URLs, hover turns pink, text is readable, footer is sticky.
</success_criteria>

<output>
After completion, create `.planning/phases/02-views-and-federation-scaffolding/02-04-SUMMARY.md`
</output>
