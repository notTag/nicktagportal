---
phase: 04-header-spacing-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shell/src/assets/main.css
  - packages/ui/src/components/TheHeader.vue
autonomous: false
requirements:
  - HEADER-01

must_haves:
  truths:
    - 'Tailwind v4 scans packages/ui/src/**/*.vue and generates CSS for classes used exclusively in that directory'
    - 'TheHeader brand name and nav links have visible, correct spacing in the browser'
    - 'Build output CSS contains gap-16, h-16, border-b, and border-t utility classes'
  artifacts:
    - path: 'apps/shell/src/assets/main.css'
      provides: 'Corrected @source directive pointing to packages/ui'
      contains: '../../../../packages/ui/src/**/*.vue'
    - path: 'packages/ui/src/components/TheHeader.vue'
      provides: 'Header with visually correct brand-to-nav spacing'
      contains: 'gap-'
  key_links:
    - from: 'apps/shell/src/assets/main.css'
      to: 'packages/ui/src/**/*.vue'
      via: '@source directive'
      pattern: '@source.*../../../../packages/ui'
    - from: 'packages/ui/src/components/TheHeader.vue'
      to: 'apps/shell/dist/assets/*.css'
      via: 'Tailwind v4 class scanning'
      pattern: 'gap-'
---

<objective>
Fix the broken Tailwind v4 `@source` directive path in main.css so that packages/ui components are scanned, then verify the header spacing is visually correct.

Purpose: The `@source` path has one too few `../` segments, resolving to `apps/packages/ui/src/` (non-existent) instead of `packages/ui/src/`. This silently drops all Tailwind utility classes used exclusively in packages/ui from the build output, including the `gap-16` that controls header spacing.

Output: Correctly scanned packages/ui with all utility classes generated, and a visually correct header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-header-spacing-fix/04-CONTEXT.md
@.planning/phases/04-header-spacing-fix/04-RESEARCH.md

<interfaces>
<!-- Current state of the two files being modified -->

From apps/shell/src/assets/main.css (line 3 -- the broken directive):

```css
@source '../../../packages/ui/src/**/*.vue';
```

From packages/ui/src/components/TheHeader.vue (line 7 -- the flex container):

```html
<div class="mx-auto flex h-16 w-full max-w-5xl items-center gap-16 px-4"></div>
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix @source directive path and verify build output</name>
  <files>apps/shell/src/assets/main.css</files>
  <read_first>
    - apps/shell/src/assets/main.css (current broken @source on line 3)
    - packages/ui/src/components/TheHeader.vue (to identify classes that should appear in build output)
    - packages/ui/src/components/TheFooter.vue (also has packages/ui-only classes like border-t)
    - .planning/phases/04-header-spacing-fix/04-RESEARCH.md (root cause analysis and fix details)
  </read_first>
  <action>
    In `apps/shell/src/assets/main.css`, change line 3 from:
    ```
    @source "../../../packages/ui/src/**/*.vue";
    ```
    to:
    ```
    @source "../../../../packages/ui/src/**/*.vue";
    ```

    This adds one additional `../` level. The CSS file is at `apps/shell/src/assets/main.css` (4 directories deep from project root), so 4 levels of `../` are needed to reach the project root before descending into `packages/ui/src/`.

    Path resolution proof:
    - Level 1 (../): apps/shell/src/
    - Level 2 (../../): apps/shell/
    - Level 3 (../../../): apps/
    - Level 4 (../../../../): project root (nicktagportal/)
    - Then: packages/ui/src/**/*.vue resolves correctly

    After the edit, run `cd apps/shell && bun run build` and verify the build output CSS file (`dist/assets/*.css`) contains these previously-missing classes:
    - `gap-16` (or the Tailwind v4 equivalent `gap: calc(var(--spacing) * 16)` or `4rem`)
    - `h-16` (or `height: calc(var(--spacing) * 16)` or `4rem`)
    - `border-b` (border-bottom)
    - `border-t` (border-top, from TheFooter)

    If build fails, check for pre-existing vue-tsc issues (noted in prior summary) and use `bunx vite build` directly if needed.

    Do NOT modify TheHeader.vue in this task -- the spacing value will be evaluated in the checkpoint task.

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal/apps/shell && bun run build 2>&1 | tail -5 && grep -c 'gap.*4rem\|gap.*calc.*16\|border-bottom\|border-top' dist/assets/*.css</automated>
  </verify>
  <acceptance_criteria>
    - apps/shell/src/assets/main.css line 3 contains exactly: @source "../../../../packages/ui/src/**/*.vue";
    - `bun run build` (or `bunx vite build`) in apps/shell succeeds
    - Built CSS file in dist/assets/*.css contains gap-related rules for 4rem/64px (previously missing)
    - Built CSS file contains border-bottom-width rule (previously missing from TheHeader border-b)
    - Built CSS file contains border-top-width rule (previously missing from TheFooter border-t)
  </acceptance_criteria>
  <done>The @source directive correctly resolves to packages/ui/src/ and Tailwind v4 generates CSS for all utility classes used in packages/ui components. Build output proves this with previously-missing classes now present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify header spacing visually</name>
  <files>packages/ui/src/components/TheHeader.vue</files>
  <read_first>
    - packages/ui/src/components/TheHeader.vue (current gap value)
  </read_first>
  <action>
    Start the dev server with `cd apps/shell && bun run dev`. Present the verification steps to the user. If the user requests a spacing adjustment (e.g., "too wide, try gap-8" or "switch to justify-between"), update the gap class in TheHeader.vue line 7 accordingly. The current value is `gap-16` (4rem/64px). Common alternatives: gap-8 (2rem/32px), gap-10 (2.5rem/40px), gap-12 (3rem/48px). Per D-04, preserve the existing nav structure: 3 tabs (Home, CLI, Playground) with gap-6 between links.
  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && grep -n 'gap-' packages/ui/src/components/TheHeader.vue</automated>
  </verify>
  <what-built>Fixed the Tailwind v4 @source scanning path so that all packages/ui utility classes are now generated in the build output. The header's gap-16 class should now produce visible 64px spacing between the brand name and nav links.</what-built>
  <how-to-verify>
    1. Run `cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal/apps/shell && bun run dev`
    2. Open http://localhost:5173 in a browser
    3. Look at the header -- you should see "Nick Tagliasacchi" on the left with clear spacing (64px / ~4rem) before the nav links (Home, CLI, Playground)
    4. Open DevTools (F12), inspect the header's inner div, and confirm:
       - The element has `gap: 4rem` (or `calc(var(--spacing) * 16)`) in computed styles
       - The `h-16` class produces `height: 4rem` on the same element
       - The `border-b` class produces a visible bottom border on the header
    5. If the spacing looks correct -- approve as-is
    6. If the spacing is too wide or too narrow -- describe what you'd prefer (e.g., "reduce to gap-8" or "use justify-between instead") and the executor will adjust TheHeader.vue accordingly
  </how-to-verify>
  <acceptance_criteria>
    - Dev server loads without errors at http://localhost:5173
    - Header inner div shows gap computed style in DevTools (not 0, not missing)
    - Header has visible bottom border (border-b class working)
    - User confirms the spacing between brand name and nav links looks correct
  </acceptance_criteria>
  <resume-signal>Type "approved" if spacing looks good, or describe the desired change (e.g., "too wide, try gap-8" or "switch to justify-between")</resume-signal>
  <done>User has approved the header spacing as visually correct, or the gap value has been adjusted per user feedback and re-approved.</done>
</task>

</tasks>

<verification>
1. Build apps/shell and confirm dist/assets/*.css contains utility classes used exclusively in packages/ui (gap-16, h-16, border-b, border-t)
2. Run dev server and visually confirm header spacing in browser
3. Inspect computed CSS in DevTools to confirm gap value is applied (not 0)
</verification>

<success_criteria>

- The @source path in main.css resolves to the correct packages/ui/src/ directory
- All Tailwind utility classes from packages/ui components appear in build output
- The header displays correct visible spacing between brand name and nav links
- User approves the visual result
  </success_criteria>

<output>
After completion, create `.planning/phases/04-header-spacing-fix/04-01-SUMMARY.md`
</output>
