---
phase: 02-views-and-federation-scaffolding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shell/vite.config.ts
  - apps/shell/src/federation/remotes.ts
  - apps/shell/package.json
autonomous: true
requirements:
  - FED-01
  - FED-02
  - FED-03
  - FED-04
  - FED-05
  - FED-06
  - FED-07
  - FED-08

must_haves:
  truths:
    - "vite.config.ts includes the federation plugin with name 'shell', empty remotes, and shared singletons"
    - "build.target is set to 'esnext' with a comment explaining the federation requirement"
    - 'build.minify is set to false with a comment about re-enabling for production'
    - 'federation/remotes.ts exports a RemoteName type and an env-aware resolveRemoteUrl function'
    - 'remotes.ts has commented examples showing how to add a remote for both dev and prod URLs'
    - 'The shell app still builds and runs successfully with the federation plugin added'
  artifacts:
    - path: 'apps/shell/vite.config.ts'
      provides: 'Federation plugin configuration'
      contains: 'federation('
    - path: 'apps/shell/src/federation/remotes.ts'
      provides: 'Env-aware URL resolver and RemoteName type'
      contains: 'resolveRemoteUrl'
  key_links:
    - from: 'apps/shell/vite.config.ts'
      to: '@originjs/vite-plugin-federation'
      via: 'import and plugin registration'
      pattern: 'import federation from.*vite-plugin-federation'
    - from: 'apps/shell/src/federation/remotes.ts'
      to: 'import.meta.env.DEV'
      via: 'environment check for URL resolution'
      pattern: "import\\.meta\\.env\\.DEV"
---

<objective>
Install @originjs/vite-plugin-federation, configure it in vite.config.ts with shared singletons and build settings, and create the federation/remotes.ts env-aware URL resolver.

Purpose: Scaffold Module Federation infrastructure so the shell app is ready to accept remote micro-frontends in the future. This is pure plumbing -- no remotes are loaded, but the configuration is in place.
Output: Updated vite.config.ts with federation plugin, new federation/remotes.ts with type-safe URL resolver.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md
@.planning/phases/02-views-and-federation-scaffolding/02-RESEARCH.md
@.claude/skills/vite-plugin-federation/SKILL.md

<interfaces>
<!-- Current vite.config.ts that will be modified -->

From apps/shell/vite.config.ts (current):

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'
import vueDevTools from 'vite-plugin-vue-devtools'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue(), tailwindcss(), vueDevTools()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
      '@ui': resolve(__dirname, '../../packages/ui/src'),
      '@types': resolve(__dirname, '../../packages/types/src'),
    },
  },
})
```

From apps/shell/package.json (current devDependencies -- federation plugin will be added here):
Uses bun as package manager. Install with: cd apps/shell && bun add -D @originjs/vite-plugin-federation@^1.4.1

From .claude/skills/vite-plugin-federation/SKILL.md:

- Host config: federation({ name, remotes, shared }) in plugins array
- build.target: 'esnext' required for top-level await
- shared: ['vue', 'vue-router', 'pinia'] for singleton sharing
- Plugin should come AFTER vue() and tailwindcss() in plugins array
- cssCodeSplit: false is only needed on remotes, NOT the host
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Install federation plugin and configure vite.config.ts</name>
  <files>
    apps/shell/vite.config.ts,
    apps/shell/package.json
  </files>
  <read_first>
    apps/shell/vite.config.ts,
    apps/shell/package.json,
    .claude/skills/vite-plugin-federation/SKILL.md,
    .planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md
  </read_first>
  <action>
    **1. Install the federation plugin:**
    ```bash
    cd apps/shell && bun add -D @originjs/vite-plugin-federation@^1.4.1
    ```

    **2. Update `apps/shell/vite.config.ts`:**

    Replace the entire file with:
    ```typescript
    import { defineConfig } from 'vite'
    import vue from '@vitejs/plugin-vue'
    import tailwindcss from '@tailwindcss/vite'
    import vueDevTools from 'vite-plugin-vue-devtools'
    import federation from '@originjs/vite-plugin-federation'
    import { resolve } from 'path'

    export default defineConfig({
      plugins: [
        vue(),
        tailwindcss(),
        vueDevTools(),
        federation({
          name: 'shell',
          remotes: {
            // Example: uncomment and configure when adding a remote
            // 'blogApp': 'http://localhost:3001/assets/remoteEntry.js',
          },
          shared: ['vue', 'vue-router', 'pinia'],
        }),
      ],
      resolve: {
        alias: {
          '@': resolve(__dirname, 'src'),
          '@ui': resolve(__dirname, '../../packages/ui/src'),
          '@types': resolve(__dirname, '../../packages/types/src'),
        },
      },
      build: {
        target: 'esnext', // Required: Module Federation uses top-level await and dynamic imports
        minify: false, // Development convenience -- re-enable for production
      },
    })
    ```

    Key points:
    - `federation()` placed LAST in plugins array (after vue, tailwindcss, vueDevTools) per RESEARCH.md pitfall #3
    - Name is `'shell'` per FED-01
    - Remotes object is empty with a commented example per FED-02
    - Shared singletons: `['vue', 'vue-router', 'pinia']` per FED-03
    - `build.target: 'esnext'` with comment per FED-04
    - `build.minify: false` with comment per FED-05
    - No `cssCodeSplit: false` on the host -- that is only needed on remotes per RESEARCH.md
    - Existing `resolve.alias` config preserved unchanged

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && grep -q 'vite-plugin-federation' apps/shell/vite.config.ts && grep -q "name: 'shell'" apps/shell/vite.config.ts && grep -q "shared: \['vue', 'vue-router', 'pinia'\]" apps/shell/vite.config.ts && grep -q "target: 'esnext'" apps/shell/vite.config.ts && grep -q "minify: false" apps/shell/vite.config.ts && grep -q "remotes:" apps/shell/vite.config.ts && grep -q 'vite-plugin-federation' apps/shell/package.json && echo "TASK 1 CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - apps/shell/package.json devDependencies contains `@originjs/vite-plugin-federation`
    - vite.config.ts contains `import federation from '@originjs/vite-plugin-federation'`
    - vite.config.ts contains `federation({` in the plugins array
    - vite.config.ts contains `name: 'shell'`
    - vite.config.ts contains `remotes: {` with a commented example inside
    - vite.config.ts contains `shared: ['vue', 'vue-router', 'pinia']`
    - vite.config.ts contains `target: 'esnext'` with comment about federation requirement
    - vite.config.ts contains `minify: false` with comment about re-enabling for production
    - vite.config.ts does NOT contain `cssCodeSplit: false` (host does not need it)
    - Federation plugin appears AFTER vue(), tailwindcss(), and vueDevTools() in the plugins array
    - Existing resolve.alias config is preserved with @, @ui, @types aliases
  </acceptance_criteria>
  <done>Federation plugin installed and configured in vite.config.ts with shell name, empty remotes, shared singletons, esnext target, and minify disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create federation/remotes.ts env-aware URL resolver</name>
  <files>
    apps/shell/src/federation/remotes.ts
  </files>
  <read_first>
    apps/shell/vite.config.ts,
    .planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md
  </read_first>
  <action>
    **Create `apps/shell/src/federation/remotes.ts`:**

    This file must be created in a new `federation/` directory under `apps/shell/src/`. Create the directory first.

    File contents (exact pattern from CONTEXT.md locked decision):
    ```typescript
    // Remote micro-frontend URL resolver
    // Extend RemoteName union when adding new remotes: 'blogApp' | 'projectsApp'
    export type RemoteName = never

    const DEV_BASE = 'http://localhost'
    const PROD_BASE = 'https://nicktag.tech'

    // Map remote names to local dev server ports
    const remotePortsDev: Record<string, number> = {
      // 'blogApp': 3001,
      // 'projectsApp': 3002,
    }

    // Map remote names to production URL paths
    const remotePathsProd: Record<string, string> = {
      // 'blogApp': '/remotes/blog',
      // 'projectsApp': '/remotes/projects',
    }

    /**
     * Resolve the remoteEntry.js URL for a given remote.
     * Uses import.meta.env.DEV to distinguish local development from production.
     *
     * Local dev: http://localhost:{port}/assets/remoteEntry.js
     * Production: https://nicktag.tech{path}/assets/remoteEntry.js
     */
    export function resolveRemoteUrl(name: RemoteName): string {
      if (import.meta.env.DEV) {
        const port = remotePortsDev[name as string]
        return `${DEV_BASE}:${port}/assets/remoteEntry.js`
      }
      const path = remotePathsProd[name as string]
      return `${PROD_BASE}${path}/assets/remoteEntry.js`
    }
    ```

    Key requirements:
    - `RemoteName` type exported as `never` -- extend when remotes are added (FED-07)
    - `resolveRemoteUrl` function uses `import.meta.env.DEV` for env detection (FED-06)
    - Commented examples in both `remotePortsDev` and `remotePathsProd` showing the dev/prod URL pattern (FED-08)
    - JSDoc comment on `resolveRemoteUrl` documenting the URL patterns
    - Two commented remote examples (`blogApp`, `projectsApp`) showing the pattern for future additions

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && test -f apps/shell/src/federation/remotes.ts && grep -q 'RemoteName' apps/shell/src/federation/remotes.ts && grep -q 'resolveRemoteUrl' apps/shell/src/federation/remotes.ts && grep -q 'import.meta.env.DEV' apps/shell/src/federation/remotes.ts && grep -q 'DEV_BASE' apps/shell/src/federation/remotes.ts && grep -q 'PROD_BASE' apps/shell/src/federation/remotes.ts && grep -q 'nicktag.tech' apps/shell/src/federation/remotes.ts && grep -q 'blogApp' apps/shell/src/federation/remotes.ts && bunx vue-tsc --noEmit --project apps/shell/tsconfig.app.json && echo "TASK 2 CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at apps/shell/src/federation/remotes.ts
    - File contains `export type RemoteName = never`
    - File contains `export function resolveRemoteUrl(name: RemoteName): string`
    - File contains `import.meta.env.DEV` for environment detection
    - File contains `DEV_BASE = 'http://localhost'`
    - File contains `PROD_BASE = 'https://nicktag.tech'`
    - File contains commented `'blogApp'` example in both remotePortsDev and remotePathsProd
    - File contains `remoteEntry.js` in the URL construction
    - File does NOT import from `@/` (it is a standalone utility)
    - `bunx vue-tsc --noEmit` passes with no type errors
  </acceptance_criteria>
  <done>federation/remotes.ts created with RemoteName type, env-aware resolveRemoteUrl function, and commented examples for dev/prod URL patterns. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `bunx vue-tsc --noEmit --project apps/shell/tsconfig.app.json` passes
2. `bun run dev` starts without errors (federation plugin does not break the dev server)
3. `bun run build` completes successfully with esnext target
4. vite.config.ts contains federation plugin with correct configuration
5. federation/remotes.ts exports RemoteName type and resolveRemoteUrl function
</verification>

<success_criteria>

- @originjs/vite-plugin-federation installed in apps/shell devDependencies
- vite.config.ts: federation plugin named 'shell', empty remotes with comment, shared singletons, esnext target, minify false
- federation/remotes.ts: RemoteName type (never), resolveRemoteUrl function with env detection, commented examples
- Shell app builds and runs without errors
- TypeScript compiles with no errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-views-and-federation-scaffolding/02-03-SUMMARY.md`
</output>
