---
phase: 02-views-and-federation-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shell/src/assets/main.css
  - apps/shell/src/data/profile.json
  - apps/shell/src/data/cliDefaultOutput.json
  - apps/shell/src/data/cliCommands.json
  - apps/shell/src/data/socialLinks.json
  - apps/shell/src/data/techSkills.json
  - packages/ui/src/components/TheHeader.vue
  - packages/ui/src/components/TheFooter.vue
  - packages/ui/src/components/SocialLinks.vue
  - packages/ui/src/index.ts
  - apps/shell/src/layouts/AppLayout.vue
autonomous: true
requirements:
  - VIEW-03

must_haves:
  truths:
    - "SynthWave '84 colors render in the browser -- dark purple background, neon pink accent, cyan and yellow accent variants"
    - "Header brand text reads 'Nick Tagliasacchi' instead of 'nick-site'"
    - "Footer displays social links (GitHub, LinkedIn, Email, Phone) centered horizontally, replacing the old 'Built with Vue 3 + Vite' text"
    - 'JSON data files exist with correct schemas for profile, CLI, social links, and tech skills'
  artifacts:
    - path: 'apps/shell/src/assets/main.css'
      provides: "SynthWave '84 color tokens"
      contains: '--color-surface: #2a2139'
    - path: 'apps/shell/src/data/profile.json'
      provides: 'Hero content data'
      contains: 'Nick Tagliasacchi'
    - path: 'apps/shell/src/data/techSkills.json'
      provides: 'Skills chip data'
      contains: 'Observability'
    - path: 'packages/ui/src/components/SocialLinks.vue'
      provides: 'Social links display with orientation'
      min_lines: 20
    - path: 'packages/ui/src/index.ts'
      provides: 'Barrel exports including SocialLinks'
      contains: 'SocialLinks'
  key_links:
    - from: 'packages/ui/src/components/TheFooter.vue'
      to: 'packages/ui/src/components/SocialLinks.vue'
      via: 'slot content injected by AppLayout'
      pattern: '<slot'
    - from: 'apps/shell/src/layouts/AppLayout.vue'
      to: 'apps/shell/src/data/socialLinks.json'
      via: 'JSON import passed as props to SocialLinks in footer slot'
      pattern: 'import.*socialLinks'
---

<objective>
Replace the Phase 1 Slate color palette with SynthWave '84, create all JSON data files that drive the views, update the header brand text, and build the SocialLinks component wired into the footer.

Purpose: Establish the visual identity and data layer that Plan 02 (views) depends on. Components and views created later will reference these color tokens and import these JSON data files.
Output: Updated main.css with SynthWave palette, 5 JSON data files in apps/shell/src/data/, SocialLinks component in packages/ui, updated TheHeader brand, updated TheFooter with social links via slot pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md
@.planning/phases/02-views-and-federation-scaffolding/02-RESEARCH.md
@.planning/phases/02-views-and-federation-scaffolding/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/ui/src/index.ts (current):

```typescript
export { default as TheHeader } from './components/TheHeader.vue'
export { default as TheFooter } from './components/TheFooter.vue'
```

From apps/shell/src/layouts/AppLayout.vue (current):

```vue
<script setup lang="ts">
import { TheHeader, TheFooter } from '@ui'
import { features } from '@/config/features'
</script>
<template>
  <div class="bg-surface text-text flex min-h-screen flex-col">
    <TheHeader />
    <main class="flex-1"><RouterView /></main>
    <TheFooter v-if="features.showFooter" />
  </div>
</template>
```

From apps/shell/src/assets/main.css (current -- to be replaced):

```css
@import 'tailwindcss';
@theme {
  --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
  --color-surface: #0f172a;
  --color-surface-raised: #1e293b;
  --color-accent: #3b82f6;
  --color-destructive: #ef4444;
  --color-text: #f1f5f9;
  --color-text-muted: #94a3b8;
  --color-border: #334155;
}
```

From packages/ui/CLAUDE.md (rules):

- Components in packages/ui must NOT import from `@/` paths
- All components use `<script setup lang="ts">`
- Use Tailwind token classes only, no hardcoded hex
- Every new component must be added to src/index.ts barrel
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: SynthWave '84 palette and JSON data files</name>
  <files>
    apps/shell/src/assets/main.css,
    apps/shell/src/data/profile.json,
    apps/shell/src/data/cliDefaultOutput.json,
    apps/shell/src/data/cliCommands.json,
    apps/shell/src/data/socialLinks.json,
    apps/shell/src/data/techSkills.json
  </files>
  <read_first>
    apps/shell/src/assets/main.css,
    .planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md,
    .planning/phases/02-views-and-federation-scaffolding/02-UI-SPEC.md
  </read_first>
  <action>
    **1. Update `apps/shell/src/assets/main.css`:**
    Replace the entire `@theme` block with SynthWave '84 values. Keep `@import 'tailwindcss';` on line 1. The new `@theme` block must be:

    ```css
    @theme {
      --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
      --color-surface: #2a2139;
      --color-surface-raised: #34294f;
      --color-accent: #ff7edb;
      --color-accent-cyan: #72f1b8;
      --color-accent-yellow: #fede5d;
      --color-destructive: #f97e72;
      --color-text: #ffffff;
      --color-text-muted: #848bbd;
      --color-border: #495495;
    }
    ```

    Two NEW tokens added: `--color-accent-cyan` and `--color-accent-yellow`. All existing token names preserved (surface, surface-raised, accent, destructive, text, text-muted, border). Only hex values change.

    **2. Create `apps/shell/src/data/` directory and all JSON files:**

    **profile.json:**
    ```json
    {
      "name": "Nick Tagliasacchi",
      "roleTrajectory": "Technical Lead \u2192 Director of Engineering",
      "bio": "Engineering leader with 13 years of software engineering experience and 6 years leading distributed teams building large-scale, security-sensitive SaaS platforms. Proven track record modernizing architecture, scaling engineering execution, and driving delivery in complex environments through strong technical judgment and cross-functional leadership."
    }
    ```

    **cliDefaultOutput.json:**
    ```json
    [{ "input": "whoami", "output": "Nick Tagliasacchi" }]
    ```

    **cliCommands.json:**
    ```json
    {
      "whoami": {
        "description": "Display current user",
        "output": "Nick Tagliasacchi"
      },
      "ls": {
        "description": "List available commands",
        "output": "auto-generated from this file"
      },
      "help": {
        "description": "Show help for a command",
        "output": "Usage: help <command>"
      }
    }
    ```

    **socialLinks.json:**
    ```json
    {
      "orientation": "center",
      "links": [
        {
          "type": "github",
          "label": "GitHub",
          "url": "https://github.com/nicktag",
          "icon": "github"
        },
        {
          "type": "linkedin",
          "label": "LinkedIn",
          "url": "https://linkedin.com/in/nicktag",
          "icon": "linkedin"
        },
        {
          "type": "email",
          "label": "Email",
          "url": "mailto:nick@nicktag.tech",
          "icon": "email"
        },
        {
          "type": "phone",
          "label": "Phone",
          "url": "tel:+1XXXXXXXXXX",
          "icon": "phone"
        }
      ]
    }
    ```

    **techSkills.json** -- Array of objects with `name`, `displayName`, `iconPath`, `category` fields. Populate with the full skills list from CONTEXT.md specifics section. Categories: Observability, CI/CD, Infrastructure, Security, Databases, Testing, Auth, Incident Mgmt, Documentation, Feature Flags, Caching, Logging, SLA/Reliability. Full list (26 entries):

    ```json
    [
      { "name": "splunk", "displayName": "Splunk", "iconPath": "/icons/skills/splunk.svg", "category": "Observability" },
      { "name": "datadog", "displayName": "DataDog APM", "iconPath": "/icons/skills/datadog.svg", "category": "Observability" },
      { "name": "jira", "displayName": "JIRA Metrics", "iconPath": "/icons/skills/jira.svg", "category": "Observability" },
      { "name": "terraform", "displayName": "Terraform", "iconPath": "/icons/skills/terraform.svg", "category": "CI/CD" },
      { "name": "atlantis", "displayName": "Atlantis", "iconPath": "/icons/skills/atlantis.svg", "category": "CI/CD" },
      { "name": "ansible", "displayName": "Ansible", "iconPath": "/icons/skills/ansible.svg", "category": "CI/CD" },
      { "name": "gitlab", "displayName": "GitLab CI", "iconPath": "/icons/skills/gitlab.svg", "category": "CI/CD" },
      { "name": "aws", "displayName": "AWS", "iconPath": "/icons/skills/aws.svg", "category": "Infrastructure" },
      { "name": "cloudflare", "displayName": "Cloudflare", "iconPath": "/icons/skills/cloudflare.svg", "category": "Infrastructure" },
      { "name": "sqs", "displayName": "SQS", "iconPath": "/icons/skills/sqs.svg", "category": "Infrastructure" },
      { "name": "kafka", "displayName": "Kafka", "iconPath": "/icons/skills/kafka.svg", "category": "Infrastructure" },
      { "name": "vault", "displayName": "HashiCorp Vault", "iconPath": "/icons/skills/vault.svg", "category": "Security" },
      { "name": "cycode", "displayName": "Cycode", "iconPath": "/icons/skills/cycode.svg", "category": "Security" },
      { "name": "casbin", "displayName": "Casbin RBAC", "iconPath": "/icons/skills/casbin.svg", "category": "Security" },
      { "name": "liquibase", "displayName": "Liquibase", "iconPath": "/icons/skills/liquibase.svg", "category": "Databases" },
      { "name": "go-testing", "displayName": "Golang Unit Tests", "iconPath": "/icons/skills/go.svg", "category": "Testing" },
      { "name": "jwt", "displayName": "JWT", "iconPath": "/icons/skills/jwt.svg", "category": "Auth" },
      { "name": "iam", "displayName": "AWS IAM", "iconPath": "/icons/skills/iam.svg", "category": "Auth" },
      { "name": "opsgenie", "displayName": "OpsGenie", "iconPath": "/icons/skills/opsgenie.svg", "category": "Incident Mgmt" },
      { "name": "slack", "displayName": "Slack", "iconPath": "/icons/skills/slack.svg", "category": "Incident Mgmt" },
      { "name": "swagger", "displayName": "Swagger", "iconPath": "/icons/skills/swagger.svg", "category": "Documentation" },
      { "name": "postman", "displayName": "Postman", "iconPath": "/icons/skills/postman.svg", "category": "Documentation" },
      { "name": "gitlab-ff", "displayName": "GitLab FF", "iconPath": "/icons/skills/gitlab.svg", "category": "Feature Flags" },
      { "name": "unleash", "displayName": "Unleash", "iconPath": "/icons/skills/unleash.svg", "category": "Feature Flags" },
      { "name": "cloudflare-cdn", "displayName": "Cloudflare CDN", "iconPath": "/icons/skills/cloudflare.svg", "category": "Caching" },
      { "name": "matomo", "displayName": "Matomo", "iconPath": "/icons/skills/matomo.svg", "category": "Logging" },
      { "name": "uptime", "displayName": "Uptime / SLAs", "iconPath": "/icons/skills/uptime.svg", "category": "SLA/Reliability" }
    ]
    ```

    Ensure all JSON files are valid JSON (no trailing commas). Use 2-space indentation for consistency with Prettier config.

  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && grep -c '#2a2139' apps/shell/src/assets/main.css && grep -c 'accent-cyan' apps/shell/src/assets/main.css && grep -c 'accent-yellow' apps/shell/src/assets/main.css && node -e "JSON.parse(require('fs').readFileSync('apps/shell/src/data/profile.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/shell/src/data/cliCommands.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/shell/src/data/cliDefaultOutput.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/shell/src/data/socialLinks.json','utf8'))" && node -e "const s=JSON.parse(require('fs').readFileSync('apps/shell/src/data/techSkills.json','utf8')); if(s.length < 25) throw new Error('Missing skills')" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - main.css contains `--color-surface: #2a2139` (SynthWave dark purple)
    - main.css contains `--color-accent: #ff7edb` (neon pink)
    - main.css contains `--color-accent-cyan: #72f1b8` (new token)
    - main.css contains `--color-accent-yellow: #fede5d` (new token)
    - main.css does NOT contain `#0f172a` (old Slate surface)
    - main.css does NOT contain `#3b82f6` (old Slate accent)
    - profile.json contains `"name": "Nick Tagliasacchi"`
    - profile.json contains `"roleTrajectory": "Technical Lead`
    - cliDefaultOutput.json contains `"input": "whoami"`
    - cliCommands.json contains keys `whoami`, `ls`, `help`
    - socialLinks.json contains `"orientation": "center"` and 4 link entries
    - techSkills.json contains at least 25 entries with `name`, `displayName`, `iconPath`, `category` fields
    - techSkills.json contains category `"Observability"` and category `"CI/CD"`
    - All 6 JSON files parse without error
  </acceptance_criteria>
  <done>SynthWave '84 palette active in main.css with 2 new accent tokens. All 5 JSON data files exist in apps/shell/src/data/ with correct schemas and content.</done>
</task>

<task type="auto">
  <name>Task 2: Header brand update, SocialLinks component, footer wiring, barrel exports</name>
  <files>
    packages/ui/src/components/TheHeader.vue,
    packages/ui/src/components/SocialLinks.vue,
    packages/ui/src/components/TheFooter.vue,
    packages/ui/src/index.ts,
    apps/shell/src/layouts/AppLayout.vue
  </files>
  <read_first>
    packages/ui/src/components/TheHeader.vue,
    packages/ui/src/components/TheFooter.vue,
    packages/ui/src/index.ts,
    packages/ui/CLAUDE.md,
    apps/shell/src/layouts/AppLayout.vue,
    apps/shell/src/data/socialLinks.json,
    .planning/phases/02-views-and-federation-scaffolding/02-UI-SPEC.md,
    .planning/phases/02-views-and-federation-scaffolding/02-CONTEXT.md
  </read_first>
  <action>
    **1. Update `packages/ui/src/components/TheHeader.vue`:**
    Change the brand text from `nick-site` to `Nick Tagliasacchi`. Only change the `<span>` text content on line 10. All other markup (classes, nav, RouterLinks) stays unchanged.

    Current: `<span class="text-text text-base font-bold">nick-site</span>`
    New: `<span class="text-text text-base font-bold">Nick Tagliasacchi</span>`

    **2. Create `packages/ui/src/components/SocialLinks.vue`:**
    This component receives data via props (NOT direct JSON import -- packages/ui must remain app-independent per packages/ui/CLAUDE.md). Uses `<script setup lang="ts">`.

    Props interface:
    ```typescript
    interface SocialLink {
      type: string
      label: string
      url: string
      icon: string
    }

    const { links = [], orientation = 'center' } = defineProps<{
      links?: SocialLink[]
      orientation?: 'left' | 'right' | 'center' | 'none'
    }>()
    ```

    Computed orientation class mapping:
    - `'center'` -> `'justify-center'`
    - `'left'` -> `'justify-start'`
    - `'right'` -> `'justify-end'`
    - `'none'` -> `'hidden'`

    Template: A `<div>` with `flex flex-wrap gap-2` and the computed justify class. Inside, `v-for` over `links` rendering `<a>` tags with:
    - `href` bound to `link.url`
    - `target="_blank"` and `rel="noopener noreferrer"` for external links
    - Text content: `link.label`
    - Classes: `text-sm text-text-muted hover:text-accent transition-colors`

    Per UI-SPEC: text labels only, no icon library. Hover changes from text-muted to accent (pink).

    **3. Update `packages/ui/src/components/TheFooter.vue`:**
    Replace the static "Built with Vue 3 + Vite" text with a `<slot />` so the consuming app can inject SocialLinks or any content.

    New TheFooter template:
    ```vue
    <template>
      <footer class="bg-surface-raised border-border border-t">
        <div class="mx-auto w-full max-w-5xl px-4 py-6">
          <slot />
        </div>
      </footer>
    </template>
    ```

    Keep `<script setup lang="ts"></script>` (empty is fine -- no imports needed).

    **4. Update `packages/ui/src/index.ts`:**
    Add SocialLinks to the barrel exports. The file should contain:
    ```typescript
    export { default as TheHeader } from './components/TheHeader.vue'
    export { default as TheFooter } from './components/TheFooter.vue'
    export { default as SocialLinks } from './components/SocialLinks.vue'
    ```

    **5. Update `apps/shell/src/layouts/AppLayout.vue`:**
    Import SocialLinks from `@ui` and socialLinks.json from `@/data/socialLinks.json`. Pass the data as props to SocialLinks inside the TheFooter slot.

    New AppLayout:
    ```vue
    <script setup lang="ts">
    import { TheHeader, TheFooter, SocialLinks } from '@ui'
    import { features } from '@/config/features'
    import socialLinksData from '@/data/socialLinks.json'
    </script>

    <template>
      <div class="bg-surface text-text flex min-h-screen flex-col">
        <TheHeader />
        <main class="flex-1">
          <RouterView />
        </main>
        <TheFooter v-if="features.showFooter">
          <SocialLinks
            :links="socialLinksData.links"
            :orientation="socialLinksData.orientation"
          />
        </TheFooter>
      </div>
    </template>
    ```

    This pattern keeps SocialLinks app-independent (receives data via props) while the JSON import lives in the shell app's layout (which is allowed to use `@/` paths). The TheFooter uses a slot so SocialLinks can be relocated later without changing the footer.

  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && grep -q 'Nick Tagliasacchi' packages/ui/src/components/TheHeader.vue && grep -q 'SocialLinks' packages/ui/src/index.ts && grep -q 'defineProps' packages/ui/src/components/SocialLinks.vue && grep -q '<slot' packages/ui/src/components/TheFooter.vue && grep -q 'socialLinksData' apps/shell/src/layouts/AppLayout.vue && bunx vue-tsc --noEmit --project apps/shell/tsconfig.app.json && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - TheHeader.vue contains `Nick Tagliasacchi` (not `nick-site`)
    - SocialLinks.vue exists in packages/ui/src/components/
    - SocialLinks.vue contains `defineProps` with `links` and `orientation` props
    - SocialLinks.vue does NOT contain any `import` from `@/` paths (no app-specific imports)
    - SocialLinks.vue contains `justify-center` and `justify-start` and `justify-end` and `hidden` in the orientation mapping
    - SocialLinks.vue contains `hover:text-accent` for link hover state
    - TheFooter.vue contains `<slot` (slot-based content injection)
    - TheFooter.vue does NOT contain `Built with Vue 3 + Vite` (old static text removed)
    - index.ts contains `export { default as SocialLinks }`
    - AppLayout.vue imports `SocialLinks` from `@ui`
    - AppLayout.vue imports `socialLinksData` from `@/data/socialLinks.json`
    - AppLayout.vue passes `:links="socialLinksData.links"` and `:orientation="socialLinksData.orientation"` to SocialLinks
    - `bunx vue-tsc --noEmit` passes with no type errors
  </acceptance_criteria>
  <done>Header brand updated. SocialLinks component created with props-based data flow. Footer uses slot pattern. AppLayout imports JSON and passes data to SocialLinks. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `bunx vue-tsc --noEmit --project apps/shell/tsconfig.app.json` passes
2. `bun run dev` starts without errors and the page renders with SynthWave purple background
3. Header shows "Nick Tagliasacchi" brand text
4. Footer shows social links (GitHub, LinkedIn, Email, Phone) centered horizontally
5. All 5 JSON data files parse as valid JSON
</verification>

<success_criteria>

- SynthWave '84 palette active: page background is dark purple (#2a2139), accent is neon pink (#ff7edb)
- Two new accent tokens available: accent-cyan (#72f1b8) and accent-yellow (#fede5d)
- Header brand reads "Nick Tagliasacchi"
- Footer renders SocialLinks with 4 social link labels, centered, with hover-to-pink transition
- All 5 JSON data files exist with correct schemas
- TypeScript compiles with no errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-views-and-federation-scaffolding/02-01-SUMMARY.md`
</output>
