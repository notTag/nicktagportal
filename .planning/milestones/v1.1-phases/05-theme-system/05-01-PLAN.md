---
phase: 05-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/shell/src/themes/types.ts
  - apps/shell/src/themes/synthwave-84.ts
  - apps/shell/src/themes/dark-modern.ts
  - apps/shell/src/themes/dark-plus.ts
  - apps/shell/src/themes/monokai-dimmed.ts
  - apps/shell/src/themes/red.ts
  - apps/shell/src/themes/solarized-dark.ts
  - apps/shell/src/themes/solarized-light.ts
  - apps/shell/src/themes/hc-dark.ts
  - apps/shell/src/themes/hc-light.ts
  - apps/shell/src/themes/index.ts
  - apps/shell/src/stores/theme.ts
  - apps/shell/src/composables/useTheme.ts
  - apps/shell/src/config/features.ts
  - apps/shell/src/assets/main.css
  - apps/shell/index.html
  - apps/shell/src/App.vue
autonomous: true
requirements:
  - THM-02
  - THM-03
  - THM-04
  - THM-06

must_haves:
  truths:
    - 'All 9 curated VSCode themes are defined as TypeScript objects conforming to a strict 16-key interface'
    - 'Selecting a theme via the Pinia store instantly changes all site colors via CSS custom properties on :root'
    - "SynthWave '84 is the default theme for first-time visitors"
    - 'Returning visitors see their saved theme immediately with no flash of wrong colors'
    - 'Tailwind utility classes (bg-surface, text-accent, etc.) work for all 16 CSS variables'
  artifacts:
    - path: 'apps/shell/src/themes/types.ts'
      provides: 'Theme interface, ThemeColors interface, ThemeId union type'
      exports: ['Theme', 'ThemeColors', 'ThemeId']
    - path: 'apps/shell/src/themes/index.ts'
      provides: 'Theme registry mapping ThemeId to Theme objects, DEFAULT_THEME_ID constant'
      exports: ['themes', 'DEFAULT_THEME_ID', 'themeList']
    - path: 'apps/shell/src/stores/theme.ts'
      provides: 'Pinia theme store with themeId, currentTheme, setTheme, previewTheme, revertPreview'
      exports: ['useThemeStore']
    - path: 'apps/shell/src/composables/useTheme.ts'
      provides: 'Composable that watches theme store and applies CSS variables to :root'
      exports: ['useTheme']
    - path: 'apps/shell/src/assets/main.css'
      provides: 'Expanded @theme block with all 16 CSS custom properties + transition rule'
      contains: '--color-surface-overlay'
    - path: 'apps/shell/index.html'
      provides: 'Inline FOUC prevention script reading localStorage and applying theme before first paint'
      contains: 'nicksite-theme'
  key_links:
    - from: 'apps/shell/src/composables/useTheme.ts'
      to: 'apps/shell/src/stores/theme.ts'
      via: 'useThemeStore() import'
      pattern: 'useThemeStore'
    - from: 'apps/shell/src/App.vue'
      to: 'apps/shell/src/composables/useTheme.ts'
      via: 'useTheme() call in setup'
      pattern: 'useTheme'
    - from: 'apps/shell/index.html'
      to: 'localStorage'
      via: 'inline script reads nicksite-theme key'
      pattern: 'nicksite-theme'
---

<objective>
Build the complete theme data layer, CSS infrastructure, and FOUC prevention for runtime theme switching.

Purpose: Establish the backbone that all UI components (ThemeDropdown, MobileMenu) will consume. After this plan, switching themes programmatically via the Pinia store works end-to-end -- colors change instantly, persist to localStorage, and restore without flash on reload. Tailwind utilities cover all 16 theme variables.

Output: 9 theme definition files, type system, Pinia store, useTheme composable, expanded CSS @theme block, FOUC prevention script, feature flag, and App.vue initialization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-system/05-CONTEXT.md
@.planning/phases/05-theme-system/05-RESEARCH.md
@.planning/phases/05-theme-system/05-UI-SPEC.md
@apps/shell/src/assets/main.css
@apps/shell/src/config/features.ts
@apps/shell/src/stores/app.ts
@apps/shell/index.html
@apps/shell/src/App.vue
@apps/shell/src/main.ts

<interfaces>
<!-- Existing patterns the executor must follow -->

From apps/shell/src/stores/app.ts:

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useAppStore = defineStore('app', () => {
  // Composition API store pattern
})
```

From apps/shell/src/config/features.ts:

```typescript
export const features = {
  showFooter: true,
} as const

export type FeatureFlags = typeof features
```

From apps/shell/src/assets/main.css:

```css
@import 'tailwindcss';
@source '../../../../packages/ui/src/**/*.vue';
@theme {
  --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
  --color-surface: #2a2139;
  /* ... 10 current variables */
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme type system and 9 curated theme definition files</name>
  <files>
    apps/shell/src/themes/types.ts,
    apps/shell/src/themes/synthwave-84.ts,
    apps/shell/src/themes/dark-modern.ts,
    apps/shell/src/themes/dark-plus.ts,
    apps/shell/src/themes/monokai-dimmed.ts,
    apps/shell/src/themes/red.ts,
    apps/shell/src/themes/solarized-dark.ts,
    apps/shell/src/themes/solarized-light.ts,
    apps/shell/src/themes/hc-dark.ts,
    apps/shell/src/themes/hc-light.ts,
    apps/shell/src/themes/index.ts
  </files>
  <read_first>
    apps/shell/src/stores/app.ts,
    apps/shell/src/config/features.ts,
    .planning/phases/05-theme-system/05-RESEARCH.md,
    .planning/phases/05-theme-system/05-UI-SPEC.md
  </read_first>
  <action>
Create the `apps/shell/src/themes/` directory. All files use TypeScript strict mode, Composition API conventions.

**types.ts** -- Define three exports (per D-13, D-15, D-27):

```typescript
export type ThemeId =
  | 'synthwave-84'
  | 'dark-modern'
  | 'dark-plus'
  | 'monokai-dimmed'
  | 'red'
  | 'solarized-dark'
  | 'solarized-light'
  | 'hc-dark'
  | 'hc-light'

export interface ThemeColors {
  readonly surface: string
  readonly surfaceRaised: string
  readonly surfaceOverlay: string
  readonly text: string
  readonly textMuted: string
  readonly textOnAccent: string
  readonly accent: string
  readonly accentCyan: string
  readonly accentYellow: string
  readonly destructive: string
  readonly link: string
  readonly linkHover: string
  readonly border: string
  readonly headerBg: string
  readonly selection: string
  readonly hover: string
}

export interface Theme {
  readonly id: ThemeId
  readonly name: string
  readonly type: 'dark' | 'light'
  readonly colors: ThemeColors
}
```

**Individual theme files** -- Each file exports a single `Theme` object. Per D-14, source colors from official VSCode theme JSON files. Per D-16, derive missing keys intelligently from nearby keys (NOT from SynthWave fallback). Per D-10, use exact VSCode display names.

Use these EXACT color values for each theme (from 05-RESEARCH.md extracted data + intelligent derivation for missing values):

**synthwave-84.ts**: `id: 'synthwave-84'`, `name: "SynthWave '84"`, `type: 'dark'`
Colors: surface=#262335, surfaceRaised=#241b2f, surfaceOverlay=#171520, text=#ffffff, textMuted=#848bbd, textOnAccent=#ffffff, accent=#ff7edb, accentCyan=#72f1b8, accentYellow=#fede5d, destructive=#fe4450, link=#f97e72, linkHover=#ff7edb, border=#495495, headerBg=#241b2f, selection=#ffffff20, hover=#37294d99

**dark-modern.ts**: `id: 'dark-modern'`, `name: 'Dark Modern'`, `type: 'dark'`
Colors: surface=#1F1F1F, surfaceRaised=#181818, surfaceOverlay=#202020, text=#CCCCCC, textMuted=#9D9D9D, textOnAccent=#FFFFFF, accent=#0078D4, accentCyan=#4daafc, accentYellow=#CCA700, destructive=#F85149, link=#4daafc, linkHover=#73c0ff, border=#2B2B2B, headerBg=#181818, selection=#04395e, hover=#2a2d2e80

**dark-plus.ts**: `id: 'dark-plus'`, `name: 'Dark+'`, `type: 'dark'`
Colors: surface=#1E1E1E, surfaceRaised=#252526, surfaceOverlay=#1E1E1E, text=#D4D4D4, textMuted=#858585, textOnAccent=#FFFFFF, accent=#0078D4, accentCyan=#4EC9B0, accentYellow=#DCDCAA, destructive=#F44747, link=#4EC9B0, linkHover=#6fe0cb, border=#303030, headerBg=#252526, selection=#094771, hover=#2a2d2e80

**monokai-dimmed.ts**: `id: 'monokai-dimmed'`, `name: 'Monokai Dimmed'`, `type: 'dark'`
Colors: surface=#1e1e1e, surfaceRaised=#272727, surfaceOverlay=#353535, text=#c5c8c6, textMuted=#8b8b8b, textOnAccent=#FFFFFF, accent=#3655b5, accentCyan=#00b0b5, accentYellow=#b5a642, destructive=#f44747, link=#6796e6, linkHover=#87b3f0, border=#3e3e3e, headerBg=#505050, selection=#3655b580, hover=#2a2d2e80

**red.ts**: `id: 'red'`, `name: 'Red'`, `type: 'dark'`
Colors: surface=#390000, surfaceRaised=#330000, surfaceOverlay=#580000, text=#F8F8F8, textMuted=#b89e9e, textOnAccent=#FFFFFF, accent=#ff6666, accentCyan=#ff9999, accentYellow=#ffcc66, destructive=#ffeaea, link=#ff6666, linkHover=#ff9999, border=#5e2020, headerBg=#770000, selection=#ff666640, hover=#5e202080

**solarized-dark.ts**: `id: 'solarized-dark'`, `name: 'Solarized Dark'`, `type: 'dark'`
Colors: surface=#002B36, surfaceRaised=#00212B, surfaceOverlay=#003847, text=#839496, textMuted=#657B83, textOnAccent=#FFFFFF, accent=#2AA198, accentCyan=#2AA198, accentYellow=#B58900, destructive=#DC322F, link=#268BD2, linkHover=#4DA3E0, border=#2b3c41, headerBg=#002C39, selection=#073642, hover=#073642cc

**solarized-light.ts**: `id: 'solarized-light'`, `name: 'Solarized Light'`, `type: 'light'`
Colors: surface=#FDF6E3, surfaceRaised=#EEE8D5, surfaceOverlay=#DDD6C1, text=#657B83, textMuted=#93A1A1, textOnAccent=#FFFFFF, accent=#AC9D57, accentCyan=#2AA198, accentYellow=#B58900, destructive=#DC322F, link=#268BD2, linkHover=#1a6da0, border=#DDD6C1, headerBg=#EEE8D5, selection=#268BD230, hover=#D3CBB880

**hc-dark.ts**: `id: 'hc-dark'`, `name: 'Dark High Contrast'`, `type: 'dark'`
Colors: surface=#000000, surfaceRaised=#0a0a0a, surfaceOverlay=#0a0a0a, text=#FFFFFF, textMuted=#CCCCCC, textOnAccent=#FFFFFF, accent=#FFA500, accentCyan=#00FFFF, accentYellow=#FFD700, destructive=#FF0000, link=#3794FF, linkHover=#5ab0ff, border=#6FC3DF, headerBg=#0a0a0a, selection=#008000, hover=#264F7880

**hc-light.ts**: `id: 'hc-light'`, `name: 'Light High Contrast'`, `type: 'light'`
Colors: surface=#FFFFFF, surfaceRaised=#F5F5F5, surfaceOverlay=#E8E8E8, text=#292929, textMuted=#666666, textOnAccent=#FFFFFF, accent=#0F4A85, accentCyan=#0F4A85, accentYellow=#B5200D, destructive=#B5200D, link=#0F4A85, linkHover=#264F78, border=#292929, headerBg=#F5F5F5, selection=#0F4A8530, hover=#CCCCCC80

**index.ts** -- Central registry re-exporting all themes (per D-17):

```typescript
import type { Theme, ThemeId } from './types'
import { synthwave84 } from './synthwave-84'
import { darkModern } from './dark-modern'
import { darkPlus } from './dark-plus'
import { monokaiDimmed } from './monokai-dimmed'
import { red } from './red'
import { solarizedDark } from './solarized-dark'
import { solarizedLight } from './solarized-light'
import { hcDark } from './hc-dark'
import { hcLight } from './hc-light'

export type { Theme, ThemeColors, ThemeId } from './types'

export const DEFAULT_THEME_ID: ThemeId = 'synthwave-84'

export const themes: Record<ThemeId, Theme> = {
  'synthwave-84': synthwave84,
  'dark-modern': darkModern,
  'dark-plus': darkPlus,
  'monokai-dimmed': monokaiDimmed,
  red: red,
  'solarized-dark': solarizedDark,
  'solarized-light': solarizedLight,
  'hc-dark': hcDark,
  'hc-light': hcLight,
}

// Ordered list for dropdown display (per D-11 / UI-SPEC ordering)
export const themeList: Theme[] = [
  synthwave84,
  darkModern,
  darkPlus,
  monokaiDimmed,
  red,
  solarizedDark,
  hcDark,
  solarizedLight,
  hcLight,
]
```

Each individual theme file exports a named const (e.g., `export const synthwave84: Theme = { ... }`). Use camelCase for the export name derived from the ID (synthwave-84 -> synthwave84, dark-modern -> darkModern, etc.).
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && npx vue-tsc --noEmit --project apps/shell/tsconfig.json 2>&1 | head -30</automated>
</verify>
<acceptance_criteria> - apps/shell/src/themes/types.ts exports `Theme`, `ThemeColors`, `ThemeId` - ThemeColors interface has exactly 16 readonly string properties: surface, surfaceRaised, surfaceOverlay, text, textMuted, textOnAccent, accent, accentCyan, accentYellow, destructive, link, linkHover, border, headerBg, selection, hover - ThemeId is a union of exactly 9 string literals - Each of 9 theme files (synthwave-84.ts, dark-modern.ts, dark-plus.ts, monokai-dimmed.ts, red.ts, solarized-dark.ts, solarized-light.ts, hc-dark.ts, hc-light.ts) exports a const satisfying the Theme interface - index.ts exports `themes` (Record of all 9), `themeList` (ordered array of 9), `DEFAULT_THEME_ID` (equals 'synthwave-84') - vue-tsc type checking passes with no errors
</acceptance_criteria>
<done>All 9 themes defined with strict types, central index exports registry and ordered list, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Pinia theme store, useTheme composable, feature flag, and App.vue initialization</name>
  <files>
    apps/shell/src/stores/theme.ts,
    apps/shell/src/composables/useTheme.ts,
    apps/shell/src/config/features.ts,
    apps/shell/src/App.vue
  </files>
  <read_first>
    apps/shell/src/stores/app.ts,
    apps/shell/src/config/features.ts,
    apps/shell/src/App.vue,
    apps/shell/src/main.ts,
    apps/shell/src/themes/types.ts,
    apps/shell/src/themes/index.ts
  </read_first>
  <action>
**stores/theme.ts** -- Pinia store (per D-21, D-25). Follows the Composition API defineStore pattern from existing `app.ts`. Must support both confirmed selection AND preview mode for arrow-key browsing (D-04):

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { themes, DEFAULT_THEME_ID } from '@/themes'
import type { ThemeId } from '@/themes/types'

const STORAGE_KEY = 'nicksite-theme'

export const useThemeStore = defineStore('theme', () => {
  const themeId = ref<ThemeId>(loadThemeId())
  const previewingId = ref<ThemeId | null>(null)

  const currentTheme = computed(
    () => themes[previewingId.value ?? themeId.value],
  )
  const confirmedThemeId = computed(() => themeId.value)
  const activeThemeId = computed(() => previewingId.value ?? themeId.value)

  function loadThemeId(): ThemeId {
    const stored = localStorage.getItem(STORAGE_KEY)
    if (stored && stored in themes) return stored as ThemeId
    return DEFAULT_THEME_ID
  }

  function setTheme(id: ThemeId) {
    themeId.value = id
    previewingId.value = null
    localStorage.setItem(STORAGE_KEY, id)
  }

  function previewTheme(id: ThemeId) {
    previewingId.value = id
  }

  function revertPreview() {
    previewingId.value = null
  }

  return {
    themeId,
    previewingId,
    currentTheme,
    confirmedThemeId,
    activeThemeId,
    setTheme,
    previewTheme,
    revertPreview,
  }
})
```

**composables/useTheme.ts** -- Create the `apps/shell/src/composables/` directory. Shell-internal composable (per D-18) that watches the store and applies CSS custom properties to `:root`:

```typescript
import { watch } from 'vue'
import { storeToRefs } from 'pinia'
import { useThemeStore } from '@/stores/theme'
import type { ThemeColors } from '@/themes/types'

const CSS_VAR_MAP: Record<keyof ThemeColors, string> = {
  surface: '--color-surface',
  surfaceRaised: '--color-surface-raised',
  surfaceOverlay: '--color-surface-overlay',
  text: '--color-text',
  textMuted: '--color-text-muted',
  textOnAccent: '--color-text-on-accent',
  accent: '--color-accent',
  accentCyan: '--color-accent-cyan',
  accentYellow: '--color-accent-yellow',
  destructive: '--color-destructive',
  link: '--color-link',
  linkHover: '--color-link-hover',
  border: '--color-border',
  headerBg: '--color-header-bg',
  selection: '--color-selection',
  hover: '--color-hover',
}

function applyTheme(colors: ThemeColors) {
  const root = document.documentElement.style
  for (const [key, cssVar] of Object.entries(CSS_VAR_MAP)) {
    root.setProperty(cssVar, colors[key as keyof ThemeColors])
  }
}

export function useTheme() {
  const store = useThemeStore()
  const { currentTheme } = storeToRefs(store)

  // Apply immediately on initialization
  applyTheme(currentTheme.value.colors)

  // Watch for changes (theme switch or preview)
  watch(currentTheme, (theme) => applyTheme(theme.colors))

  return store
}
```

**config/features.ts** -- Add `showThemePicker: true` per D-07, following existing `showFooter` convention:

```typescript
export const features = {
  showFooter: true,
  showThemePicker: true,
} as const

export type FeatureFlags = typeof features
```

**App.vue** -- Initialize useTheme in setup so it starts watching immediately. Import useTheme and call it:

```vue
<script setup lang="ts">
import AppLayout from './layouts/AppLayout.vue'
import { useTheme } from '@/composables/useTheme'

useTheme()
</script>

<template>
  <AppLayout />
</template>
```

  </action>
  <verify>
    <automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && npx vue-tsc --noEmit --project apps/shell/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/shell/src/stores/theme.ts exports `useThemeStore` via defineStore('theme', ...)
    - Store exposes: themeId, previewingId, currentTheme, confirmedThemeId, activeThemeId, setTheme, previewTheme, revertPreview
    - Store reads from localStorage key 'nicksite-theme' on init
    - Store defaults to 'synthwave-84' when localStorage is empty
    - apps/shell/src/composables/useTheme.ts exports `useTheme` function
    - useTheme contains `CSS_VAR_MAP` with all 16 entries matching ThemeColors keys
    - useTheme calls `applyTheme` immediately and watches `currentTheme`
    - apps/shell/src/config/features.ts contains `showThemePicker: true`
    - apps/shell/src/App.vue imports and calls `useTheme()`
    - vue-tsc type checking passes
  </acceptance_criteria>
  <done>Theme store manages confirmed + preview state, composable applies CSS vars to :root reactively, feature flag enabled, App.vue initializes theme on mount</done>
</task>

<task type="auto">
  <name>Task 3: CSS @theme expansion, transition rule, and FOUC prevention script</name>
  <files>
    apps/shell/src/assets/main.css,
    apps/shell/index.html
  </files>
  <read_first>
    apps/shell/src/assets/main.css,
    apps/shell/index.html,
    apps/shell/src/themes/synthwave-84.ts,
    apps/shell/src/themes/types.ts,
    .planning/phases/05-theme-system/05-UI-SPEC.md
  </read_first>
  <action>
**main.css** -- Expand the `@theme {}` block from 10 to 16 CSS custom properties (per D-15, D-26). Use SynthWave '84 values as defaults. Add transition rule and no-transition suppression class (per D-12, UI-SPEC transition section).

Replace the entire file with:

```css
@import 'tailwindcss';

@source '../../../../packages/ui/src/**/*.vue';

@theme {
  --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;

  /* Surface colors */
  --color-surface: #262335;
  --color-surface-raised: #241b2f;
  --color-surface-overlay: #171520;

  /* Text colors */
  --color-text: #ffffff;
  --color-text-muted: #848bbd;
  --color-text-on-accent: #ffffff;

  /* Accent colors */
  --color-accent: #ff7edb;
  --color-accent-cyan: #72f1b8;
  --color-accent-yellow: #fede5d;

  /* Semantic colors */
  --color-destructive: #fe4450;
  --color-link: #f97e72;
  --color-link-hover: #ff7edb;

  /* UI chrome */
  --color-border: #495495;
  --color-header-bg: #241b2f;
  --color-selection: #ffffff20;
  --color-hover: #37294d99;
}

/* Theme transition for smooth color switching (D-12: 250ms ease-in-out) */
*,
*::before,
*::after {
  transition:
    color 250ms ease-in-out,
    background-color 250ms ease-in-out,
    border-color 250ms ease-in-out;
}

/* Suppress transitions on page load to prevent initial color animation */
.no-transition,
.no-transition *,
.no-transition *::before,
.no-transition *::after {
  transition: none !important;
}
```

NOTE: The surface value changes from `#2a2139` to `#262335` and surfaceRaised from `#34294f` to `#241b2f` to match the actual SynthWave '84 VSCode theme JSON values (per D-14). The old values were approximations. The destructive color changes from `#f97e72` to `#fe4450` (actual VSCode errorForeground). The old `#f97e72` moves to `--color-link` (textLink.foreground).

**index.html** -- Add inline FOUC prevention script in `<head>` before any rendering (per D-19). Also add `class="no-transition"` to `<html>` to suppress transition on load, and remove it after first paint. The script must be plain JS (no imports, no modules, no TypeScript).

Replace the entire file with:

```html
<!doctype html>
<html lang="en" class="no-transition">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <title>nick-site</title>
    <script>
      ;(function () {
        var K = 'nicksite-theme'
        var D = 'synthwave-84'
        var T = {
          'synthwave-84': {
            '--color-surface': '#262335',
            '--color-surface-raised': '#241b2f',
            '--color-surface-overlay': '#171520',
            '--color-text': '#ffffff',
            '--color-text-muted': '#848bbd',
            '--color-text-on-accent': '#ffffff',
            '--color-accent': '#ff7edb',
            '--color-accent-cyan': '#72f1b8',
            '--color-accent-yellow': '#fede5d',
            '--color-destructive': '#fe4450',
            '--color-link': '#f97e72',
            '--color-link-hover': '#ff7edb',
            '--color-border': '#495495',
            '--color-header-bg': '#241b2f',
            '--color-selection': '#ffffff20',
            '--color-hover': '#37294d99',
          },
          'dark-modern': {
            '--color-surface': '#1F1F1F',
            '--color-surface-raised': '#181818',
            '--color-surface-overlay': '#202020',
            '--color-text': '#CCCCCC',
            '--color-text-muted': '#9D9D9D',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#0078D4',
            '--color-accent-cyan': '#4daafc',
            '--color-accent-yellow': '#CCA700',
            '--color-destructive': '#F85149',
            '--color-link': '#4daafc',
            '--color-link-hover': '#73c0ff',
            '--color-border': '#2B2B2B',
            '--color-header-bg': '#181818',
            '--color-selection': '#04395e',
            '--color-hover': '#2a2d2e80',
          },
          'dark-plus': {
            '--color-surface': '#1E1E1E',
            '--color-surface-raised': '#252526',
            '--color-surface-overlay': '#1E1E1E',
            '--color-text': '#D4D4D4',
            '--color-text-muted': '#858585',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#0078D4',
            '--color-accent-cyan': '#4EC9B0',
            '--color-accent-yellow': '#DCDCAA',
            '--color-destructive': '#F44747',
            '--color-link': '#4EC9B0',
            '--color-link-hover': '#6fe0cb',
            '--color-border': '#303030',
            '--color-header-bg': '#252526',
            '--color-selection': '#094771',
            '--color-hover': '#2a2d2e80',
          },
          'monokai-dimmed': {
            '--color-surface': '#1e1e1e',
            '--color-surface-raised': '#272727',
            '--color-surface-overlay': '#353535',
            '--color-text': '#c5c8c6',
            '--color-text-muted': '#8b8b8b',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#3655b5',
            '--color-accent-cyan': '#00b0b5',
            '--color-accent-yellow': '#b5a642',
            '--color-destructive': '#f44747',
            '--color-link': '#6796e6',
            '--color-link-hover': '#87b3f0',
            '--color-border': '#3e3e3e',
            '--color-header-bg': '#505050',
            '--color-selection': '#3655b580',
            '--color-hover': '#2a2d2e80',
          },
          red: {
            '--color-surface': '#390000',
            '--color-surface-raised': '#330000',
            '--color-surface-overlay': '#580000',
            '--color-text': '#F8F8F8',
            '--color-text-muted': '#b89e9e',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#ff6666',
            '--color-accent-cyan': '#ff9999',
            '--color-accent-yellow': '#ffcc66',
            '--color-destructive': '#ffeaea',
            '--color-link': '#ff6666',
            '--color-link-hover': '#ff9999',
            '--color-border': '#5e2020',
            '--color-header-bg': '#770000',
            '--color-selection': '#ff666640',
            '--color-hover': '#5e202080',
          },
          'solarized-dark': {
            '--color-surface': '#002B36',
            '--color-surface-raised': '#00212B',
            '--color-surface-overlay': '#003847',
            '--color-text': '#839496',
            '--color-text-muted': '#657B83',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#2AA198',
            '--color-accent-cyan': '#2AA198',
            '--color-accent-yellow': '#B58900',
            '--color-destructive': '#DC322F',
            '--color-link': '#268BD2',
            '--color-link-hover': '#4DA3E0',
            '--color-border': '#2b3c41',
            '--color-header-bg': '#002C39',
            '--color-selection': '#073642',
            '--color-hover': '#073642cc',
          },
          'solarized-light': {
            '--color-surface': '#FDF6E3',
            '--color-surface-raised': '#EEE8D5',
            '--color-surface-overlay': '#DDD6C1',
            '--color-text': '#657B83',
            '--color-text-muted': '#93A1A1',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#AC9D57',
            '--color-accent-cyan': '#2AA198',
            '--color-accent-yellow': '#B58900',
            '--color-destructive': '#DC322F',
            '--color-link': '#268BD2',
            '--color-link-hover': '#1a6da0',
            '--color-border': '#DDD6C1',
            '--color-header-bg': '#EEE8D5',
            '--color-selection': '#268BD230',
            '--color-hover': '#D3CBB880',
          },
          'hc-dark': {
            '--color-surface': '#000000',
            '--color-surface-raised': '#0a0a0a',
            '--color-surface-overlay': '#0a0a0a',
            '--color-text': '#FFFFFF',
            '--color-text-muted': '#CCCCCC',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#FFA500',
            '--color-accent-cyan': '#00FFFF',
            '--color-accent-yellow': '#FFD700',
            '--color-destructive': '#FF0000',
            '--color-link': '#3794FF',
            '--color-link-hover': '#5ab0ff',
            '--color-border': '#6FC3DF',
            '--color-header-bg': '#0a0a0a',
            '--color-selection': '#008000',
            '--color-hover': '#264F7880',
          },
          'hc-light': {
            '--color-surface': '#FFFFFF',
            '--color-surface-raised': '#F5F5F5',
            '--color-surface-overlay': '#E8E8E8',
            '--color-text': '#292929',
            '--color-text-muted': '#666666',
            '--color-text-on-accent': '#FFFFFF',
            '--color-accent': '#0F4A85',
            '--color-accent-cyan': '#0F4A85',
            '--color-accent-yellow': '#B5200D',
            '--color-destructive': '#B5200D',
            '--color-link': '#0F4A85',
            '--color-link-hover': '#264F78',
            '--color-border': '#292929',
            '--color-header-bg': '#F5F5F5',
            '--color-selection': '#0F4A8530',
            '--color-hover': '#CCCCCC80',
          },
        }
        var id = localStorage.getItem(K) || D
        var t = T[id] || T[D]
        var s = document.documentElement.style
        for (var k in t) s.setProperty(k, t[k])
        // Remove no-transition class after first paint to enable smooth switching
        requestAnimationFrame(function () {
          requestAnimationFrame(function () {
            document.documentElement.classList.remove('no-transition')
          })
        })
      })()
    </script>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

The double-RAF pattern ensures the browser has painted with the correct theme before transitions are enabled, preventing the initial load color animation.
</action>
<verify>
<automated>cd /Users/nicktag/Code/Projects/nicktagtech/nicktagportal && bun run --cwd apps/shell build 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - apps/shell/src/assets/main.css @theme block declares exactly 16 --color-\* variables plus --font-sans - main.css contains `transition: color 250ms ease-in-out, background-color 250ms ease-in-out, border-color 250ms ease-in-out` - main.css contains `.no-transition` class with `transition: none !important` - apps/shell/index.html contains `<html lang="en" class="no-transition">` - index.html contains inline `<script>` with `nicksite-theme` localStorage read - index.html FOUC script contains all 9 theme IDs with all 16 CSS variable keys each - index.html contains `requestAnimationFrame` for removing no-transition class - `bun run --cwd apps/shell build` completes without errors
</acceptance_criteria>
<done>CSS @theme block expanded to 16 variables, transition rule enables smooth 250ms color switching, FOUC prevention script applies saved theme before first paint, build succeeds</done>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit --project apps/shell/tsconfig.json` -- zero type errors
2. `bun run --cwd apps/shell build` -- successful build
3. `bun run --cwd apps/shell dev` -- site loads, SynthWave '84 colors visible, no console errors
4. Manual: Open browser console, run `document.documentElement.style.setProperty('--color-surface', '#1F1F1F')` -- background changes instantly (proves CSS variable override works)
</verification>

<success_criteria>

- 9 theme definition files exist with strict TypeScript types (16 color keys each)
- Theme registry exports ordered list and lookup map
- Pinia store supports confirmed + preview theme selection
- useTheme composable reactively applies CSS variables to :root
- Tailwind generates utilities for all 16 CSS variables (bg-surface-overlay, text-link, bg-selection, bg-hover, etc.)
- FOUC prevention script applies saved theme before first paint
- Feature flag `showThemePicker` exists and is `true`
- App.vue initializes the theme system on mount
- Build passes, type checking passes
  </success_criteria>

<output>
After completion, create `.planning/phases/05-theme-system/05-01-SUMMARY.md`
</output>
