---
phase: 06-skills-diamond-wall
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - apps/shell/src/views/SkillsView.vue
autonomous: false
requirements: [SKL-01, SKL-02, SKL-03, SKL-04, SKL-05, SKL-06, SKL-07, SKL-08]

must_haves:
  truths:
    - 'User navigates to /skills and sees toolbar at top + diamond wall filling remaining space'
    - 'Diamonds animate in with staggered entrance, then scroll continuously'
    - 'Hovering a diamond pauses row, lifts diamond, shows info panel with name/years/category'
    - 'Category pills filter by dimming non-matching diamonds to 30% opacity'
    - 'Search filters by displayName, dimming non-matches'
    - 'Proficiency toggle switches between Uniform/Glow/Size/Fill visualization'
    - 'Wall renders correctly on mobile (48px diamonds, 3 rows), tablet (56px, 4 rows), desktop (80px, 4-5 rows)'
    - 'prefers-reduced-motion disables scroll and entrance animations'
  artifacts:
    - path: 'apps/shell/src/views/SkillsView.vue'
      provides: 'Full page assembly of SkillsToolbar + DiamondWall'
      min_lines: 20
  key_links:
    - from: 'apps/shell/src/views/SkillsView.vue'
      to: 'apps/shell/src/components/skills/SkillsToolbar.vue'
      via: 'direct import and render'
      pattern: 'import.*SkillsToolbar'
    - from: 'apps/shell/src/views/SkillsView.vue'
      to: 'apps/shell/src/components/skills/DiamondWall.vue'
      via: 'direct import and render'
      pattern: 'import.*DiamondWall'
---

<objective>
Assemble SkillsView page from toolbar and diamond wall components, then visually verify the complete Skills Diamond Wall experience.

Purpose: Final integration and visual verification. After this plan, the /skills route shows a fully functional diamond wall with scrolling, hover effects, filtering, search, proficiency modes, and responsive layout.

Output: Complete SkillsView.vue replacing the stub from Plan 01, and user-verified visual experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-skills-diamond-wall/06-CONTEXT.md
@.planning/phases/06-skills-diamond-wall/06-UI-SPEC.md
@.planning/phases/06-skills-diamond-wall/06-01-SUMMARY.md
@.planning/phases/06-skills-diamond-wall/06-02-SUMMARY.md

@apps/shell/src/views/SkillsView.vue
@apps/shell/src/components/skills/SkillsToolbar.vue
@apps/shell/src/components/skills/DiamondWall.vue

<interfaces>
<!-- Components available for assembly -->

From apps/shell/src/components/skills/SkillsToolbar.vue:

- No props required (reads from Pinia store internally)
- Contains: CategoryPills, search input, ProficiencyToggle

From apps/shell/src/components/skills/DiamondWall.vue:

- No props required (reads from Pinia store and techSkills.json internally)
- Contains: Multiple DiamondRow instances with responsive row count
- Uses useIntersectionObserver for entrance animation trigger
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble SkillsView with toolbar and diamond wall</name>
  <files>
    apps/shell/src/views/SkillsView.vue
  </files>
  <read_first>
    apps/shell/src/views/SkillsView.vue
    apps/shell/src/components/skills/SkillsToolbar.vue
    apps/shell/src/components/skills/DiamondWall.vue
    apps/shell/src/views/HomeView.vue
    apps/shell/src/layouts/AppLayout.vue
    .planning/phases/06-skills-diamond-wall/06-UI-SPEC.md
  </read_first>
  <action>
Replace the SkillsView.vue stub with the full page assembly. Per D-22, there is NO page title. The toolbar sits at the top and the diamond wall fills the remaining space below.

```vue
<script setup lang="ts">
import SkillsToolbar from '@/components/skills/SkillsToolbar.vue'
import DiamondWall from '@/components/skills/DiamondWall.vue'
</script>

<template>
  <div class="bg-surface flex min-h-full flex-col">
    <SkillsToolbar />
    <div class="flex-1 overflow-hidden pt-6">
      <DiamondWall />
    </div>
  </div>
</template>
```

Key layout details:

- `min-h-full` ensures the view fills the AppLayout main area (which has `flex-1 overflow-y-auto`)
- `flex flex-col` with toolbar above and `flex-1` wall below per D-22 layout
- `bg-surface` for page background (theme-aware via CSS custom property)
- `overflow-hidden` on the wall container prevents horizontal scroll from diamond rows that extend past viewport
- `pt-6` adds 24px (lg spacing token) gap between toolbar and wall per UI-SPEC spacing scale
- No page title, no heading element -- toolbar and wall speak for themselves (D-22)
- Wall spans full viewport width (no max-width container) -- toolbar is constrained to max-w-5xl but wall is not (per D-02 and UI-SPEC responsive contract)

Verify TypeScript compiles:

```
cd apps/shell && bunx vue-tsc --noEmit
```

Verify build succeeds:

```
cd apps/shell && bunx vite build
```

  </action>
  <verify>
    <automated>grep 'SkillsToolbar' apps/shell/src/views/SkillsView.vue && grep 'DiamondWall' apps/shell/src/views/SkillsView.vue && cd apps/shell && bunx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/shell/src/views/SkillsView.vue imports SkillsToolbar from `@/components/skills/SkillsToolbar.vue`
    - apps/shell/src/views/SkillsView.vue imports DiamondWall from `@/components/skills/DiamondWall.vue`
    - apps/shell/src/views/SkillsView.vue template contains `<SkillsToolbar />` and `<DiamondWall />`
    - apps/shell/src/views/SkillsView.vue uses `bg-surface` for themed background
    - apps/shell/src/views/SkillsView.vue does NOT contain an h1 or page title (D-22)
    - `cd apps/shell && bunx vue-tsc --noEmit` exits 0
    - `cd apps/shell && bunx vite build` exits 0
  </acceptance_criteria>
  <done>SkillsView.vue assembles SkillsToolbar and DiamondWall into a full-page layout with no page title. TypeScript compiles and Vite builds successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Skills Diamond Wall</name>
  <files>apps/shell/src/views/SkillsView.vue</files>
  <action>
Start the dev server with `cd apps/shell && bunx vite --open` and navigate to /skills. Present the full Skills Diamond Wall to the user for visual and interaction verification. This checkpoint covers all 8 SKL requirements and all 24 locked decisions (D-01 through D-24).
  </action>
  <what-built>
Complete Skills Diamond Wall implementation across 3 plans:
- /skills route with toolbar (category pills, search, proficiency mode toggle) and continuously scrolling diamond wall
- Diamonds as 45-degree rotated squares with Devicon SVG icons
- Multiple rows scrolling at different speeds (parallax depth)
- Hover: row pauses, diamond lifts, info panel shows name/years/category
- Filter: category pills and search dim non-matching diamonds to 30% opacity
- Proficiency modes: Uniform (default), Glow, Size, Fill
- Staggered entrance animation via IntersectionObserver
- Responsive: 48px/3 rows mobile, 56px/4 rows tablet, 80px/4-5 rows desktop
- "Skills" link in header nav and mobile hamburger menu
  </what-built>
  <how-to-verify>
Start dev server: `cd apps/shell && bunx vite --open`

**1. Navigation (SKL-01, D-20, D-21)**

- Header shows: Home | Skills | CLI | Playground
- Click "Skills" -- navigates to /skills
- On mobile viewport (resize to <640px), open hamburger menu -- "Skills" link visible between Home and CLI

**2. Diamond Layout (SKL-02, D-01, D-05)**

- Diamonds appear as rotated squares (diamond shapes) with icons inside
- Icons appear upright (counter-rotated) within the diamond frame
- Multiple rows with alternating offset for masonry tight-packing

**3. Continuous Scroll (D-02, D-03, D-04)**

- Rows scroll slowly left-to-right
- Different rows scroll at different speeds
- Scrolling loops seamlessly (no visible jump/stutter)

**4. Entrance Animation (SKL-03, SKL-08, D-08)**

- On page load, diamonds animate in with staggered fade+scale
- After entrance, continuous scroll begins

**5. Hover Interaction (SKL-04, D-06, D-07)**

- Hover a diamond: row pauses, diamond lifts up, glow effect appears
- Info panel slides in below with skill name, years, and category
- Mouse leave: diamond returns, info panel fades, scroll resumes

**6. Filter/Search (SKL-07, D-15-D-18)**

- Category pills: tap to filter. Non-matching diamonds dim to ~30% opacity. Wall keeps scrolling.
- Tap "All" to reset. Multiple categories can be active simultaneously.
- Type in search box: filters by skill name (debounced). Dimming, no layout change.
- Combined: category + search both apply.

**7. Proficiency Modes (SKL-06, D-12, D-13, D-19)**

- Default is "Uniform" -- all diamonds identical
- Switch to "Glow" -- diamonds show accent-colored glow proportional to years
- Switch to "Size" -- diamonds scale up proportional to years
- Switch to "Fill" -- diamond background opacity increases with years
- Hover info panel always shows years regardless of mode

**8. Responsive (SKL-05)**

- Desktop (>1024px): 80px diamonds, 4-5 rows
- Tablet (640-1024px): 56px diamonds, 4 rows, toolbar in single row
- Mobile (<640px): 48px diamonds, 3 rows, toolbar stacked

**9. Accessibility**

- Enable "prefers-reduced-motion" in browser DevTools -- all animations should stop, diamonds display statically
  </how-to-verify>
  <verify>User confirms visual and interaction verification by typing "approved"</verify>
  <done>User has approved the complete Skills Diamond Wall visual experience covering all 8 SKL requirements.</done>
  <resume-signal>Type "approved" if the diamond wall looks and behaves correctly, or describe any issues to fix.</resume-signal>
  </task>

</tasks>

<verification>
1. Navigate to /skills -- page loads with toolbar and diamond wall
2. Diamonds scroll continuously, pause on hover, show info panels
3. Category pills and search filter by dimming
4. Proficiency toggle switches visual modes
5. Responsive layout adapts across breakpoints
6. prefers-reduced-motion disables animations
7. Header and mobile menu both show Skills link in correct position
</verification>

<success_criteria>

- User approves visual verification
- /skills route fully functional with all 8 SKL requirements satisfied
- All 24 locked decisions (D-01 through D-24) implemented
- Diamond wall renders on mobile, tablet, and desktop
- Animations respect prefers-reduced-motion
  </success_criteria>

<output>
After completion, create `.planning/phases/06-skills-diamond-wall/06-03-SUMMARY.md`
</output>
